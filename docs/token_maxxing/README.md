# Token-Maxxing Sessions

A log of deliberately high-token-consumption sessions and the real value each delivered.

| Date | Title | Achievement | Doc |
|------|-------|-------------|-----|
| 2026-09-12 | UI Primitives YAGNI | Gave `components/ui` the dead-code pass it had never had (worker `ui-primitives-yagni` of `tokenmax-orch-2026-09-12`; the repo's largest component tree, 5,234 lines, made safe by the prior day's 205-test coverage sweep): all 17 source files / 44 exports classified by a throwaway TS-compiler-AST import-graph script (kept out of git) built to defeat the four known verifier traps — twin names resolved per module, relative imports resolved, barrel re-exports mapped transitively through `index.ts`, multiline imports read as AST — positive-controlled before trusting (synthetic probe read DEAD, through-barrel attribution verified), landing commit `dba8f17` (9 files, +25/−46, all under `components/ui`): 3 zero-importer internal types un-exported (`AppShellScreen`, `SheetProps`, `PanelParam`), 3 speculative/test-only `className` props deleted per the RunDateLink round-3 rule ("a prop with no caller is a second way to render, waiting") — `AppShell`'s set at 0 of 11 call sites and caught by `tsc` after the JSX-attribute scanner missed a `{...props}` spread in its own test, `EmptyState`'s and `Field`'s set only by their own tests, cases removed (suite 5,093 → 5,091, reconciled to the digit; `EmptyState`'s `cn` import fell with it) — the `TAB_BAR_*` test seams deliberately KEPT on scope grounds (consumer `tests/tabbar.geometry.test.ts` lives outside the directory fence), the barrel's claims verified rather than trusted (15/15 re-exports pulled through, `Toast` confirmed still nonexistent), 3 doc-claim drifts inside the directory measured and fixed with their 2026-09-12 measure dates ("ten client components" → 32; AppShell "five pages" → 9 pages + layout + 2 loadings; Sheet "two call sites" → 3 named), and 2 false alarms dismissed with reasons (`NumberInput.ref` inherited via shared `InputProps`; `buttonClasses` is a function, not JSX) — the honest verdict being that the tree was already unusually clean: 3 un-exports, 3 speculative props, 3 stale counts, verified-true everywhere else (tsc clean excl. known typegen noise; components/ui 203/203; full sweep 5,089 + 2 pre-existing MemoryTable parallel-load flakes reproduced on clean HEAD via stash, 20/20 serially; eslint+prettier clean) | [link](./2026-09-12-ui-primitives-yagni.md) |
| 2026-09-12 | Docs Nina Persona Audit | Audited `docs/nina/persona.md` (29.8KB prose "canon") claim-by-claim against the implementation it describes (`lib/nina/persona.ts`, `tuning.ts`, `prompts/*`, schema/`drizzle/`, `app/admin/*`, git log) and landed ten corrections in one commit (`69fe78c`, 1 file, +51/−37) — the headline being a whole missing persona (the Instructor: sixth relationship level in `NINA_RELATIONSHIPS`/`NINA_ADDRESS`/`NINA_RELATIONSHIP_BLOCKS` with the gated `INSTRUCTOR_COACHING` block, absent from a doc that said "Five levels"), plus the body repeal corrected to four traits from three in two places, `horny` added to the trait defaults, the three appearance paragraphs restored to verbatim copies of `NINA_BODY_SENTENCES`/`NINA_FACE`/`NINA_DEFAULT_OUTFIT` (script-verified exact match, so future drift is diffable), the verbosity dial's row rewritten to the mechanism that shipped after a *declined* proposal, rotted pinned version constants unpinned, the purged `nina_turns` revision row removed, and the status line promoted draft → living canon — while the doc's spine (voice examples, anger ladder bands, trait/dial tables, defaults contract, twelve F34 repeal sites) verified accurate | [link](./2026-09-12-docs-nina-persona-audit.md) |
| 2026-09-12 | Lib Admin Pkg-Readme Compaction | Compacted `lib/admin/.workflows/package_readme.md` 1,300 → 890 lines (−32%; one commit `e9d2dc3`, 651+/1,061−, markdown-only) by rebuilding it from a verification base instead of trimming it: mechanical export extraction across all 24 `lib/admin` modules, a repo-wide import census of every `@/lib/admin/*` specifier, and targeted reads of every documented module — zero claims restated on trust. The file had missed ~72 commits since 2026-09-09, so stale material ran deep: the 8-action memory era replaced by the real 4 actions + `memoryDeleteSchema` union (its "append comes first" invariant dropped as folklore per the code's own header), the deleted `resetNinaTuningAction` and `/admin/photos` route removed, chat-photo actions 4 → 6 and album actions 7 → 15 with the write-time dedupe and chat-photo adoption documented for the first time, `avatarRegisterSchema`'s "no live caller" annotation corrected (`explorer/thumbnail.ts` imports it), and the 4 stale export lines the 2026-09-11 dead-exports sweep left behind fixed — while coverage ADDED six undocumented modules (`folderOps`, `imageGenTestView`, `textModelActions`, `shareToNina`, imagegen map rows, the `?view=media` switch); the ~160-line inline per-task changelog became one Recent Changes entry pointing at `git log -- lib/admin`, killing the recurring-context cost the idea named | [link](./2026-09-12-pkg-readme-lib-admin.md) |
| 2026-09-12 | Nina package_readme Compaction | First 2026-09-12 worker session (`tokenmax-orch-2026-09-12`, slug `pkg-readme-nina-cmp`): compacted the largest bloated package readme — `components/nina/.workflows/package_readme.md`, loaded into context on every nina task — from 1,017 lines / 96.7 KB to 713 lines / 61.5 KB (−36%, commit `11f5689`, 514+/818−), but only after a claim-by-claim verification sweep (dated `git log` on `components/nina` + `lib/nina` since 2026-09-10, per-commit diffs ×7, greps of every named export/prop/constant/suite/boundary claim) found ~15 claims the 2026-09-11 work had falsified or drifted — the "node cannot render a client component so no component tests" story (31/32 files now carry happy-dom suites), the retired 90 s `NINA_TURN_STALE_MS` give-up identity (now `NINA_BACKGROUND_BUDGET_MS` 240 s), the gone Prompt section in NinaJobDetail, ChatChrome's vanished 32 px disc exception, the "media-dedupe phase 2 of 4" note (4/4 complete), and a `lucide-static` dependency that does not exist among them — while the full skill skeleton (Overview, 32-file module map, turn/keyboard/dedup/about/sidebar sections, export table, deps, concurrency, errors, performance, usage+gotchas) survived and the duplicated double changelog (~85 lines) became one per-task "Recent changes" record | [link](./2026-09-12-pkg-readme-nina-cmp.md) |
| 2026-09-12 | Lib Nina Todos Archive | Completed the package-ledger archival trilogy: `lib/nina/.workflows/todos.md` — the one ledger yesterday's root/nina-component archive sessions left untouched (703 lines vs siblings of 53–157) — compressed 703 → 90 lines by archiving all 35 completed tasks as one-line entries (TaskID, title, plan set + phase, bracketed plan path; all 35 paths verified to resolve on disk across the two bases — 25 package-relative adopted copies + 10 repo-root-relative source plans, the 10 adopted copies proven never to have existed rather than lost), with the 3 still-open Open/Outstanding obligations kept verbatim as line suffixes and the one live task (P1-NIN-A018) byte-identical; zero dedup needed (36/36 unique IDs at the per-entry uniq -c gate) and Quick Stats verified honest before the rewrite (Completed: 35 was the real count, unlike the root session's stale 37); plus a tooling correction recorded: `todos.py plan-of` is a pure ID→path deriver that exits 0 for any canonical ID whether or not the file exists, so on-disk resolution is the only real reachability receipt (1 file, +45/−658, commit `d81aa7f`) | [link](./2026-09-12-todos-nina-lib-archive.md) |
| 2026-09-12 | Lib Nina Package Readme Compaction | Compacted the repo's worst-bloated package doc — `lib/nina/.workflows/package_readme.md` 3,767 → 557 lines (−85%, ~6.2 lines/file vs scripts' 8.4 despite a ~90-file package) — to yesterday's scripts calibration: ~2,700 lines of per-phase implementation narratives (accreted after every set since 2026-09-05, duplicating `.workflows/plan/` and these session docs) cut, and every surviving claim re-derived from the tree with ~40 exhaustive greps (no head cutoffs), which caught the stale ones mid-rewrite — both "no importer" claims (tuning-aware exports, `captionNinaPhoto`) falsified as wired long ago, tuning/shortcut reads re-anchored from actions.ts into turnrun.ts STEP 2's four-way Promise.all, image budgets corrected (anchored timeout 220s→235s, run budget 240s→255s, plus 150s unanchored/290s worker), "six generations a day" replaced with `NINA_IMAGE_DAILY_CAP` fallback 30 (env-tunable via `ninaImageDailyCap()`, clamped 1..200, counting failures), `maxDuration=300` re-censused to four segments, soft-delete predicate re-censused to ten WHEREs across nine functions, and the per-migration applied/not-applied table dropped entirely because it rots per deploy (the rule stays) — with every volatile count stamped with its 2026-09-12 measure date (1 file, +516/−3,726, commit `0b7020e`) | [link](./2026-09-12-pkg-readme-nina-lib.md) |
| 2026-09-12 | Docs-Design Staleness Audit & Compaction | Continued yesterday's dead-links session into the content half: every checkable claim in `docs/design/DESIGN_INTEGRATION.md`, `docs/design-brief.md` and `docs/design/tokens.css` verified against the tree before editing — the token mirror came back TRUE for all 25 tokens in both schemes (the 4 radii match by value under `--radius-*` literals, a naming wrinkle now documented at the trap site after the audit's own naive name-keyed diff skipped them) — then 9 stale claims fixed (the brief's bare retired-record citation on line 4, the `list_projects` re-pull trap, "Thresholds are fixed" vs `lib/charts/zones.ts`'s per-run HRmax bounds, R-32's threshold location since R-42, the 4-tab bar that ships as five with Nina, the ten records that ship as eleven, `--miss` now consumed as the zone fallback, a "Still open" heading holding only closed items, plus a shipped-deltas landing note) and 3 duplications compacted (+39/−30, comment-only in tokens.css) — with both brief edits above line 175 kept line-count-neutral because `components/admin/DialSlider.tsx:64` cites that line by number | [link](./2026-09-12-docs-design-audit.md) |
| 2026-09-12 | Root Package Readme Compaction | First-ever compaction of the root `.workflows/package_readme.md` (1359 → 769 lines, −43%; +422/−1012, commit `f6e595a`) — twelve per-phase "Recent changes" entries (2026-09-05..09-11, roughly half the file) folded into one rolling summary under a new written one-paragraph-per-wave policy (durable statements in the body sections and the constants' docstrings, narrative in git history) — with every surviving claim re-verified against the current tree, finding major drift the doc had never recorded: geometry re-based to the compact bar (`TAB_BAR_HEIGHT_PX` 58→39, outer 40, new `TAB_BAR_CONTENT_DROP_CSS`, `COMPOSER_RESTING_PX` 68→60, `BOTTOM_GAP.chat` 7.5rem→7rem = 60+8+32+12, FALLBACK 100; R5/`18b0c58` named), the safe-area inset actually INSIDE `composerBottomCss`'s gate (the doc asserted the opposite) with three new exports (`composerPadBottomCss`/`panelBottomCss`/`attachStripPadBottomCss`) documented, bar state re-homed to `AppShell`'s `NinaBarProvider` with the sidebar rail's "up" dispatching toggle (`listScrollRef`/`onScrollToTop` gone), the route table fixed in both directions (+`/nina/jobs` and `[id]`, −merged-away `/admin/photos`, `/nina` maxDuration 300 not 60, 19 pages not 16), counts re-measured (22 lib dirs not 23, 13 components dirs not 15, ~270 suites not ~100 with ~74 happy-dom — the harness postdates the doc's "cannot be asserted at all" claims, which were reworded honestly rather than deleted), the install contract updated for `ADMIN_PWA_ICONS` + `admin/apple-icon.png` (the same-art known-cost note gone) with the viewport merge now cited from Next's `mergeViewport`, and the retired `RECONCILIATION_v0.1.0.md`/`ROADMAP_v0.1.0.md` citations repointed at git history (prettier --check passes; docs-only diff — root worker of the day's six-readme fan-out, scripts/ left alone at 226 lines) | [link](./2026-09-12-pkg-readme-root.md) |
| 2026-09-12 | Docs Architecture & README Refresh | Closed the drift window the 2026-09-11 architecture synthesis opened the same day it predicted drift (worker `docs-architecture-readme-refresh` of `tokenmax-orch-2026-09-12`): extracted the exact removal checklist from the 9 code commits after the docs' baseline `e0add93` (7 removed functions, 28 de-exported symbols), falsified the assignment's named suspect (`6fe5f82` predates the baseline), and fixed every real hit — architecture.md's F03/§6 rewritten around the surviving `getObservedMaxHrRun`, §4.2 invariant 1 naming the allow-list the guard actually enforces (`isUniqueViolation`, not the phantom `requireAdmin()` member), §8's dead `/admin/photos` route struck (pre-existing error, `746e454` purged it before the doc existed) — plus README's 3,566 → 5,093 tests (full suite re-run green today), "six a day" → `NINA_IMAGE_DAILY_CAP`, "Seven tabs" → six, and all three v0.2.0 mentions → v1.0.0 (3 files, +43/−29; 70/70 cited paths exist, stale-string sweep clean) | [link](./2026-09-12-docs-architecture-readme-refresh.md) |
| 2026-09-12 | Admin Package-Readme Compact | Opened the package-readme compaction campaign with `components/admin` (worker session, coordinator `tokenmax-orch-2026-09-12`): 1,950 → 1,291 lines (−34%, +1,144/−1,803, one commit) while ADDING three sections that didn't exist (The text-model select, `/admin/image-generation`, top-level Gotchas) — where the real value was the stale-claim harvest the verify-then-compact order forced, ~15 facts fixed, headlined by: the module map naming two files the /admin/memory one-table rebuild deleted (`MemoryLedger`/`MemorySlots`) and omitting their replacements (`MemoryTable`/`TextModelSelect`); the central "no test files and why that is correct rather than a gap" claim falsified by vitest's own include pattern and 27 colocated happy-dom suites (rewritten as a three-layer testing story); the "migration 0003 applied to no live database" 2026-09-04 snapshot replaced with a measured read-only production count (20/20 migrations, `nina_avatars` 36 rows, `nina_folders` 0); ImageGenPanel's pre-template section brought current (editable prompt template `0b85fcc`, model select `1e9b4d5`, ✕ clears `94fdb1a`, test panel deriving from `lib/admin/imageGenTestView.ts`); and the `/admin/nina` "Image collection" nav rename reflected — with every test-enforced guard compressed-but-kept and the load-bearing constants claim-by-claim grep-verified (`NINA_CHAT_PHOTO_PAGE_SIZE=48`, `ADMIN_LEDGER_PAGE=200`, `ADMIN_FOLDER_OP_MAX_IDS=500`, `NINA_TRAITS=12`, `NINA_DIALS=4`, …); pattern written down for the other six readmes (lib/nina's is 3,767 lines): measure, don't extend, and stamp volatile numbers with their measure date | [link](./2026-09-12-pkg-readme-admin-cmp.md) |
| 2026-09-12 | Lib DB Package Readme Compaction & Re-verify | Compacted `lib/db/.workflows/package_readme.md` 1,098 → 657 lines (−40% of the file every persistence-layer task loads as recurring context; ~520 lines of per-phase narratives became a ~20-row documentation-history table), where the real value was the verification harvest — every claim re-derived rather than copy-edited, yielding a deleted type still in the Exit list (`MonthlyTotal`), a whole table missing from the "complete" inventory (`appSettings`/0020), a module-private union listed as exported (`AdapterAccountType`), db-instance importer counts corrected to measured 60 source / 72 static / 90 incl. dynamic (the "five files" claim → ten, via the Nina turn pipeline's direct imports; first-pass line-grep 76/62 was inflated by comment quotes, settled by specifier-exact parsing), the scoping invariant's "exactly one exception" fixed to two (`listActiveUserIds`), the cascade gotcha rewritten around 0013's `set null`, retired ROADMAP/RECONCILIATION pointers repointed at the ten live R-x rulings, and migration names realigned to the live journal — capped by a read-only production measurement that found the one true deploy defect the file never mentioned: `0011_rare_blockbuster` (DROP `nina_memory_facts.confidence`) is unapplied AND watermark-skipped, so `db:migrate` skips it silently forever (column inert in prod; repair shape recorded: hand DROP + constructed journal fix, memory updated); plus `schema.ts`/`queries.ts` header comments aligned so source and readme state the same facts — an adversarial reviewer subagent's independent re-derivation caught seven further discrepancies (four substantive) before the single commit `356c5f4` (3 files, +186/−624; typegen+tsc+prettier green) | [link](./2026-09-12-pkg-readme-lib-db.md) |
| 2026-09-11 | Docs-Design Dead-Link Repair | Repaired the one place the 204fd34 retirement of the v0.1.0 contract trio missed: `docs/design/DESIGN_INTEGRATION.md`'s dead citations of the retired `RECONCILIATION_v0.1.0.md`/`ROADMAP_v0.1.0.md` now resolve via a "Where the retired citations point" note (exact `git show 204fd34^:` retrieval), with the banner re-pointed at the live token sheet + the shipped components carrying R-41–R-46, Roadmap §4.6/§4.7 re-pointed at `lib/badges/*` and the generate-badge style contract, and two stale "Still open" items closed — keeping the doc's full record text, since it is the canonical home of rulings R-29–R-35 (the record's Part IV delegates them here) plus a comment-only fix to `docs/design/tokens.css`'s header (2 files, +48/−18; 17/17 cited paths resolve) | [link](./2026-09-11-docs-design-dead-links.md) |
| 2026-09-11 | Lib Nina Queries YAGNI | Finally acted on the most-deferred cleanup in token-maxxing history (flagged 4–5 prior sessions, each deferred for missing test coverage — coverage that landed earlier the same day): a genuine unused-export audit of all 114 `lib/nina/queries.ts` exports (84 functions + 30 interfaces, word-boundary-grep'd repo-wide after proving no namespace/barrel imports exist) found exactly ONE dead export — removed `countNinaSessionMessages` (23 deletions), deliberately superseding task #136's recorded keep-decision — plus refiled the 5 `[x]`-completed tasks misfiled in `lib/nina/.workflows/todos.md`'s Active section into Completed with two timestamps repaired from commit archaeology (typecheck clean; full suite 265 files/5,107 tests green) | [link](./2026-09-11-lib-nina-queries-yagni.md) |
| 2026-09-11 | Scripts Package Hygiene | The day's only scripts/-scoped worker session — a 2-file, +244/−17 doc diff that replaced two pieces of silently-wrong bookkeeping with measured facts: reconstructed P1-SC-A000's abandoned closure from git (root cause: /implement Step 3 skipped by design in `aee6b75`, landing `62727a2` never wrote the block) and measured the outcome instead of minting a moot "run --apply" follow-up (read-only: `nina_shortcuts` 25 + `nina_memory_facts` 3 = the original 28 conserved, migration already complete), and expanded `package_readme.md` 7 → 226 lines into a real map of all 27 scripts in five groups with the directory's six standing rules — catching en route that the old readme's central warning ("dedupe --apply has NOT yet been run") was stale per `c2c2ca5` and the live DB (48/52 hashed, 17 repointed) | [link](./2026-09-11-scripts-package-hygiene.md) |
| 2026-09-11 | Nina Todos TaskID De-dup & Archive | Fixed the package's one real TaskID collision at its root: two parallel 2026-09-09 plan sets each minted `P1-CN-A001` and their adopted copies add/add'd on the same ID-derived path, so for two days the keyboard task's Plan pointer described the *window-pin's* plan — the window pin re-minted `P1-CN-A005` (Former ID stamped, copy `git mv`'d, `Depends on` re-pointed), the keyboard copy restored byte-exact from `a1bed18`, all four living `package_readme.md` references corrected, and a tooling blind spot recorded: `todos.py validate` exits 0 on a same-file ID collision (`tasks()` calls the repeat a "log echo"), so the binding gate is a per-entry `grep \| sort \| uniq -c`; plus all six completed tasks archived to single lines (the two live on-device Outstanding checklists kept), Active Tasks/Completed view scannable at 0 active | [link](./2026-09-11-todos-nina-archive.md) |
| 2026-09-11 | Plans Archive | Gave `docs/plans/` the archival mechanism it lacked (worker session under the day's orchestrator): all 36 SHIPPED/SHIPPED+AMENDED plans — 19,224 lines, proven byte-preserved as 36 pure 100% renames — moved to `docs/plans/archive/` behind a new charter README, the 3-week-old F16 filename collision settled by renumbering the race's second claimer to `F16b-upload-kind-swap.md` (evidence-dated 23 minutes after the first), and ~30 live plan-path reference strings in code (source/test/config comments, 4 CI guards' error messages, 6 badge-art tool headers) plus all living docs repointed at the archive — path substrings only, zero behavior change; flagged that main's `format:check` CI gate is already red with ~48 inherited dirty files | [link](./2026-09-11-docs-plans-archive.md) |
| 2026-09-11 | Lib Admin Dead-Exports Audit | First dead-code pass over `lib/admin`'s two largest files (`filetree.ts` 1,151 ln, `ninaAlbumActions.ts` 1,224 ln), run as a worker session under the day's parallel coordinator: a name-grep-refusing census (74 exports enumerated, every import site extracted symbol-by-symbol across ~20 files, every grep hit manually classified code-vs-prose) found the dead surface is type-only — 15 type exports named by zero importers whose only external hits were comment prose — unexported in two 1-file commits (6d9d6f8, 422daa5) totalling 15 keyword-only changed lines; all 15 Server Actions, all 20 functions and all 13 constants proven live, test-only callers kept, gates green (typegen+tsc+eslint, 51 files/1,128 tests serially) | [link](./2026-09-11-lib-admin-dead-exports.md) |
| 2026-09-11 | Todos Root Archive & TaskID Dedupe | First worker-mode session for the coordinator fan-out: shrank the root `.workflows/todos.md` 844 → 84 lines by archiving all 41 completed tasks into its previously-empty Archive section (one line each — TaskID, title, plan set + phase, bracketed plan path; all 40 paths verified to exist; stale Quick Stats "Completed 37" corrected to 41) and deduped the TaskIDs a 2026-09-09 mint race had double-assigned across plan sets — the SIMPLIFY pair renamed to verified-unspent A040/A041 (highest+1 + `git log --all -S`), plans `git mv`'d as R100, 9 readme sites re-pointed by content; final audit: A040/A041 at exactly 11 sites, A025/A026 at exactly 4, all legitimately SEARCH's | [link](./2026-09-11-todos-root-archive.md) |
| 2026-09-11 | Components Dead-Code Sweep | The day's first YAGNI/dead-code session (worker of `tokenmax-orch-2026-09-11`): swept all 27 exported symbols across the 17 files of `components/{extract,auth,push,runs,insights}` + `lib/derived/` via repo-wide word-boundary greps (barrel re-exports and `next/dynamic` string refs: none) and full-file reads to separate real callers from prose mentions — every one of the 17 files is alive, and exactly 4 export keywords had zero importers repo-wide (`ExtractionStatusState`, `RunListRow`, `WeekDivider`, `PushSetupFallback`), each un-exported with nothing deleted, while 4 more zero-importer symbols were kept as documented contract-test seams; plus a stale "currently-no-op" claim about `onRunCommitted` in `invalidate.ts`'s header corrected (265 files/5,107 tests green) | [link](./2026-09-11-components-dead-code-b.md) |
| 2026-09-11 | Lib DB Queries YAGNI Dead-Code Removal | Audited all 80 exports of `lib/db/queries.ts` (1,914 lines) for callers repo-wide (word-boundary greps + a dynamic-channel sweep: namespace imports, bracket access, `db.query.*`, CI — none; one `.workflows/` doc-mention false positive corrected before it drove a decision) and removed the 6 genuinely dead functions + 1 orphaned interface in 4 gated commits (`getMonthlyTotals`+`fillZeroMonths`, `getObservedMaxHr`+`getObservedMaxHrExcludingRun`, `listExtractions`, `deletePhoto`) with every companion: the `ALLOWED_UNSCOPED` guard list 4→3 and its exact-mirror test, the reviewed-only invariant-completion list 15→14→12, rollups/ownership/extractions/integration suites — 8 files, −315 lines, queries.ts → 1,777, default suite 5,107→5,093 (delta reconciled to the digit) — zero schema.ts/migration/table/column change, all six `ci:*` boundary guards green, and the kept-and-noted list (getInsight, ownership vocabulary, type-only exports) written down for the next pass | [link](./2026-09-11-lib-db-queries-yagni.md) |
| 2026-09-11 | Components Dead-Code Sweep (app/ + ui/charts/review/profile/share/trends) | First symbol-level YAGNI pass over `app/` + six component directories — earlier surveys had ruled out orphaned *files* but never asked which exports have zero callers: 9 exported symbols de-exported (Button's 5 prop/variant types, Field's 2, plus module-private `chipClasses` and `Flag`) and 6 dead barrel re-exports dropped (commit `eb93607`, 7 files, +46/−38), found by a TypeScript-compiler-API export extractor feeding a four-layer verifier (per-symbol word-boundary grep, comment/path-string classifier, multiline-aware barrel-importer scan, positive-controlled against a planted dead symbol) and confirmed by a 0-candidate post-removal re-sweep — with both loud false alarms correctly dismissed (the review/ vs ui/ `SplitsTable`+`ZoneBar` twins, relative-imported and documented as deliberate; the 42-importer barrel itself), and the Chip/Flag test pins reworked onto the public surface with coverage intact (9+8 tests, before = after; suite 265 files/5,107 green) | [link](./2026-09-11-components-dead-code-a.md) |
| 2026-09-11 | Admin PhotoReference False-Positive Fix | Small targeted fix, committed directly to `main` (no branch): closed the one follow-up the admin-explorer-upload-tests session deliberately left open — `tests/admin.photoReference.test.ts`'s 29 `readFileSync`/`toContain` source-text assertions (the day's second string-matching false positive, after `admin.mediaPane.test.ts`) moved to 3 new real DOM tests in `PhotoReferencePicker.test.tsx`, one mutation-verified, plus a stale `existsSync` phase-4/5 guard dropped and the file's now-inaccurate "no jsdom" docstring rewritten (44 → 45 tests; full suite 265 files/5,107 tests green) | [link](./2026-09-11-admin-photoreference-false-positive-fix.md) |
| 2026-09-11 | Lib Admin Action Tests | Finally closed the follow-up deferred by all 3 prior sessions: first-ever runtime execution of the admin security boundary (`requireAdmin` — redirect/notFound split, id-first order, 401/404, empty-ADMIN_EMAILS crash) and the 9 fully-untested `lib/admin` action/store files plus the 5 thrice-deferred avatar actions, ~180 tests (174 measured) in the repo's real-query-layer-over-fakeDb posture — including a real 23505 unique-violation through drizzle's error wrap via a new additive `fakeDb.enqueueError`, and honesty pins like `registerNinaAvatarsAction`'s `skipped` counting storage conflicts, not join misses (suite: 4,191 → 4,365 tests) | [link](./2026-09-11-lib-admin-action-tests.md) |
| 2026-09-11 | UI Primitives Tests | Closed the shared design-system layer's zero coverage — all 16 untested files in `components/ui` (14 primitives + the `usePanelParam`/`useSavePhoto` hooks, a gap named on prior sessions' menus and passed over every time) with 205 real DOM tests across 16 new co-located files (~2,740 lines), the rendered-scenario complement to the repo's node-env text-scan suites rather than a duplicate — including the iOS-keyboard bug's absence proven in a rendered Sheet sequence and the native dialog's no-redundant-ARIA rule (suite: 4,191 → 4,396 tests) | [link](./2026-09-11-ui-primitives-tests.md) |
| 2026-09-11 | Architecture Reference | Closed the follow-up all five prior 2026-09-11 sessions deferred (5× deferred, 0× acted on): read all 36 `docs/plans/*.md` in full (~19.2k lines), cross-checked each against the current implementation (672 commits, 28 tables, 204 lib modules, ~3.6k tests), and synthesized `docs/architecture.md` (622 lines) — the system as it actually exists, including a per-plan cross-reference (§13) marking all 36 docs SHIPPED / SHIPPED+AMENDED / SUPERSEDED with what diverged | [link](./2026-09-11-architecture-reference.md) |
| 2026-09-11 | Nina Remaining Component Tests | Closed the day's open arc in one batch session: 25 new co-located test files / 193 tests (~3,300 lines) covering every still-untested `components/nina` component — the flagged-since-session-2 search field and SessionList/SessionRow/ChatChrome/NinaAboutScreen included — with real collaborators over mocks (real `next/image`, search, crop math, `planSessionRemoval`), taking the nina directory to 31 files / 313 tests all green (full suite: 4,403) | [link](./2026-09-11-nina-remaining-component-tests.md) |
| 2026-09-11 | Admin Explorer & Upload-Hook Tests | Finished the admin component-coverage sweep in the day's first worker-mode session: first real suites for all 18 remaining untested surfaces — the two components the prior session had to stub (`PhotoMoveBar`, `FolderMenu`), the ~447-line `useFolderUpload` upload state machine (renderHook + gated promises + `@vercel/blob/client` mock, four-lane bound mutation-checked), and the admin control panels (auto-save commit matrix, escalating-poll image-gen loop, optimistic tables, `photoIcons` glyph table) — 324 tests across 18 new files (suite: 4,191 → 4,515), plus a second string-matching false positive exposed (`tests/admin.photoReference.test.ts`) | [link](./2026-09-11-admin-explorer-upload-tests.md) |
| 2026-09-11 | Admin File-Explorer Tests | Closed the admin FileExplorer subsystem's total lack of component-level coverage — flagged in two prior sessions' follow-ups as "the largest untouched real gap" — with 119 real DOM tests (RTL + happy-dom) across all 9 files (`FileExplorer.tsx` + 8 `components/admin/explorer/*.tsx`), proving a pre-existing string-matching suite was a coverage false positive (suite: 4,072 → 4,191 tests) | [link](./2026-09-11-admin-file-explorer-tests.md) |
| 2026-09-11 | Nina Message-Actions-Sheet Tests | Closed the gap named as a follow-up in all 3 prior 2026-09-11 docs and acted on by none of them: 30 sanity-checked tests for `MessageActionsSheet.tsx`'s menu/edit/retry/resend/delete paths, including the retryable-vs-non-retryable failed-message distinction and the three edit-refusal shapes (suite: 4,042 → 4,072 tests) | [link](./2026-09-11-nina-message-actions-sheet-tests.md) |
| 2026-09-11 | Admin Folder-Actions Tests | Corrected a false-positive "zero coverage" survey of `components/admin`/`lib/admin` (tests live centrally under `tests/`, not co-located), then closed a real gap: 23 sanity-checked tests for the 6 previously-untested folder-maintenance Server Actions in `ninaAlbumActions.ts` (suite: 4,019 → 4,042 tests) | [link](./2026-09-11-admin-folder-actions-tests.md) |
| 2026-09-11 | Nina Composer/Bubble/List Component Tests | Continued the day's component-testing push onto the 3 files the first session flagged as untested — `Composer.tsx`'s upload/dedupe pipeline, `MessageBubble.tsx`'s gesture wiring, and `MessageList.tsx`'s grouping/composition logic — adding 48 sanity-checked tests across 3 new files (suite: 3,971 → 4,019 tests) | [link](./2026-09-11-nina-composer-bubble-list-tests.md) |
| 2026-09-11 | Nina Chat Component Tests | Stood up React component testing from zero (RTL + happy-dom installed and wired into Vitest) and wrote 23 sanity-checked tests covering `ChatScreen.tsx` and `NinaSidebar.tsx`, the app's two largest/highest-churn UI surfaces, previously covered only by manual testing | [link](./2026-09-11-nina-chat-component-tests.md) |
