# Token-Maxxing Sessions

A log of deliberately high-token-consumption sessions and the real value each delivered.

| Date | Title | Achievement | Doc |
|------|-------|-------------|-----|
| 2026-09-12 | Badges/Records Combined Package Readme | Wrote the standing doc the gamification pair was owed after the same-day YAGNI sweep (worker `tokenmax-pkg-readme-badges-records` of `tokenmax-orch-2026-09-12`; commit `1f20955`, 1 new file, +338, markdown-only) — as ONE combined `package_readme.md` placed at `lib/badges/.workflows/` with the placement decision recorded IN the doc (lib/ is not a package, so the header declares itself lib/records' readme too, with a do-not-fork-silently instruction), because the honest shape is the seam no per-package doc can hold: one trigger contract (`lib/derived/invalidate.ts`'s `onRunCommitted`, records-then-badges ordering, load-bearing), one art pipeline (tools/ decks), one CI guard (`check-badge-art.mjs`), a shared `runs.started_at` column read two deliberate ways (string lexical compare in badges vs seconds-past-midnight int via compute.ts's private `clockToSeconds`), and the strict one-way dependency (badges imports records; records must never import badges, enforced by precedent not lint); content: the two write shapes (records = recompute-and-wholesale-replace, absence is meaningful; badges = append-only never-revoked ledger, ON DELETE SET NULL per R-22), 8 standing rules (R-42 threshold single-sourcing incl. Nina reading `BADGE_THRESHOLDS.lateStartAfter`; pure→orchestrator→server-only-gateway layering; catalog order everywhere; type-only-importable records/catalog.ts for the strip-types backfill script), file maps for all 17 files, the commit pipeline as ASCII data, read paths (/me, run detail, Nina), reverse wiring (Nina's 4 modules, `scripts/backfill-record-keys.mjs` under `node --experimental-strip-types`, `scripts/backfill-badge-run-ids.mjs` whose `PERIOD_KEYS` literal is test-guarded, next.config.ts immutable headers), 13 gotchas, a 12+4-file testing map, and a verification stamp dated today; the method was the value: all 17 source files read in full before writing, a broken first reverse-dep grep returning empty was caught as the tool's bug (an empty result contradicting a known fact is your bug, not a fact) and redone with exact-quote greps, a dynamic-`await import()` pass found the 3 gateway/db test files the static grep missed (`tests/badges.gateway.test.ts`, `tests/records.gateway.test.ts`, `tests/db.queries.recordsAndBadges.test.ts` — `await import` behind `vi.mock`, recorded in the doc's gotchas as a verifier trap), and two draft claims were falsified by direct source checks before landing — the "one non-cascade FK" framing copied from invalidate.ts's own stale comment corrected to schema.ts:859's TWO deliberate SET NULL FKs (badges.run_id AND nina_messages.run_id), and the gateway count corrected from a plausible 7 to the counted 10 reads + 2 writers; gates: prettier --check green (format:check covers .md), tsc/vitest not applicable to a markdown-only diff, tree clean; not merged — the coordinator lands | [link](./2026-09-12-pkg-readme-badges-records.md) |
| 2026-09-12 | UI/Share Comment-Citation Polish | Closed the two named leftovers the same-day YAGNI sessions recorded as follow-ups (worker `ui-share-polish` of `tokenmax-orch-2026-09-12`; commit `95c99e1`, 1 file, +8/−8, every changed line inside a `/** */` block of `lib/share/copy.ts` — zero runtime effect): verified the mid-flight upstream move before acting on it (the coordinator's claim that `41cbed7` already fixed the `Card.test.tsx:90` react/no-unescaped-entities pair confirmed against the actual commit diff — `7'22"` → `7&apos;22&quot;` — so that half was skipped, not re-fixed into a merge collision), then falsified the naming session's premise while verifying the second: `SheetSource` is NOT a nonexistent component (as the review-yagni doc twice records) but a live export at `components/review/ScreenshotStrip.tsx:129` with two consumers (`SplitsTable:238`, `ZoneBar:211`) — the drift was a symbol written in path form, and the comment's underlying claim re-verified TRUE (`ScreenshotStrip.tsx:145` renders the byte-identical "tap to zoom" that `PHOTO_ZOOM_HINT` holds), so the keep-in-sync warning was kept, now findable; the same citation sweep caught a second same-class defect nobody had named — line 7's `app/s/[token]/copy.ts` never resolved, the module living under `app/(public)/s/[token]/` since f11; gates: prettier --check (proves the comment blocks parse), both cited paths ls-verified on disk, zero other unresolved backticked refs in the file; the real guarantee (importing `PHOTO_ZOOM_HINT` into ScreenshotStrip so the literals cannot drift) recorded as the follow-up, deliberately untouched — a component render path is collision territory in a fan-out | [link](./2026-09-12-ui-share-polish.md) |
| 2026-09-12 | Review Follow-up Cleanup | Closed both leftovers the day's earlier sessions recorded but could not act on (worker `review-followup-cleanup` of `tokenmax-orch-2026-09-12`): the write-only `changedFieldPaths` off `RunChangeEvent` — found write-only by the schema-misc dead-export sweep, unactable because its writer lib/review sat outside that sweep's assignment — removed in one coordinated 4-file commit (`63bfbf6`, −14 lines, zero insertions: interface field + doc line off `lib/derived/invalidate.ts`, the property off commit.ts's `onRunCommitted()` event literal while the local `Object.keys(corrections)` const SURVIVES as R-7's recordCorrections gate — the actual record of what moved, untouched; the exact-match assertion narrowed and the passthrough-pinning test deleted since F06/F09 demonstrably never read it), proven by repo-wide `tsc --noEmit` on a REQUIRED-field removal (mechanical proof no reader existed anywhere — app code, tests, scripts) plus git-show verification (invalidate.ts 0 hits, commit.ts exactly 2: const def + gate read, commit test 0); and the only two untested lib/review files — loadReview.ts and actions.ts, recorded by the review-yagni sweep as exactly that gap — covered with 25 tests (+557, commit `644ec6c`): loadReview under a queries-mocked-but-draft.ts-FOR-REAL posture (stubbing the hydrators would let provenance rot while the suite stayed green — the mapping IS the feature) pinning the parsedSession-vs-stored-rows provenance split with named spot checks keeping the oracle honest (distanceKm 10.67, startTime '07:07' narrowed, postWorkoutHr positional [End 185, 1 MIN 162]), §8's blank draft through the SAME branch with its occurredOn doubling as the clock passthrough, ownership null before any second query, by-reference vendor JSON + corrections log (`toBe` identity — never re-parsed, never inferred from); actions.ts per the share.actions posture (lazy `await import()`, all collaborators mocked, redirect mock throwing the Next-docs NEXT_REDIRECT-shaped error so success = the action's promise REJECTING — neither redirect nor auth failures are ever caught), pinning scheduled-not-awaited as an observed property (`emitRunCommitted` untouched at action-return; exactly one callback on isNewRun, none on a post-review edit), the four revalidatePath calls before redirect, the Nina reaction's exact payload + its failure logged-never-thrown backstop + it.each over three malformed payloads all reading occurredOn '' rather than throwing inside after, and INVARIANT A (requireUserId strictly before commitReview, payload raw) — every lib/review file now has a dedicated suite; gates: typegen + tsc exit 0 + prettier clean on all five touched files, 197/197 across the nine review+derived suites (172 prior + 25 new) re-measured after the last test edits; the one-liner re-add path recorded (commit.ts still computes Object.keys(corrections), so the data never left — only the unread copy did); not merged — coordinator lands | [link](./2026-09-12-review-followup-cleanup.md) |
| 2026-09-12 | Scripts Package-Readme Verification Pass | The seventh and last of the day's pkg-readme passes (worker `tokenmax-scripts-readme-compact` of `tokenmax-orch-2026-09-12`; the six-worker fan-out took root + five sub-packages and deliberately left `scripts/` for this), CLOSING THE SET — all seven package_readme.md files now carry a same-week verification pass — on a target born compact (226 lines, expanded 2026-09-11 by the scripts-package-hygiene session, `b049bb7`), so the mandate resolved to the compaction discipline rather than shrinking: every claim re-checked against the tree (all seven `check-*.mjs` guards read IN FULL not grepped — the method that caught drifts a name-grep never would, 22 npm-script mappings plus two no-entry negatives, the 27-file inventory reconciled both ways, negative claims re-proved — `@/scripts` imports empty across lib/app/components, the worker the only .ts, `.github/workflows/nina-image.yml` exists — and the zero-import rule spot-checked on all five named lib modules, one type-only import exactly as the strip-types rule allows), landing 7 corrections in one commit (`e8d5f48`, 55+/27−, 226 → 254 lines — the honest direction was +28, not −1000): headline the perceptual twin-gate constants' second home corrected to `lib/nina/perceptual.ts` (the write-time twin check, born `2df323f`) — `perceptualSign.ts` is the signer and NEVER held them (`git log -S PERCEPTUAL_MAX_DHASH` on that path is empty), and the lib side carries a fifth constant `PERCEPTUAL_MAX_SIG16` the sweep side lacks, so the doc's old "these must move together" citation would have sent the next loosening edit to a file that silently would not propagate; the client-secret guard's THIRD raw-read exemption `lib/nina/vision.test.ts` (`78f1a9c`, same-day — a colocated test's dummy-key default that broke the guard on every push) plus `NEXT_PUBLIC_` "nowhere at all" scoped to where the guard actually scans; the data-layer allowlist named in full (`isUniqueViolation`, `listActiveUserIds` beside `getRunByShareToken` — the old text mirrored the guard's own success line instead of its allowlist); the openrouter guard's source-extension scoping recorded as an instance of the prose-defense law with BOTH its mechanisms stated (comment stripping OR extension scoping); precision fixes (seed-demo's three real npm entries, the hosting workflow named, a Notes section transplanting the one-paragraph-per-wave anti-re-inflation rule plus doc history) — with every other claim verified true and left standing (f08's `PaceHrChartInner.tsx` waiver, f11's four-file route set, llm-payload's nine entry points, all flag defaults, the capture toolkit's numbers, every dated production measurement kept honest by its stamp) and two meta-lessons recorded: a `head -12` truncation nearly wrote off a TRUE replyToId claim (verification greps need `grep -c` or no head — same failure mode as the inventory-grep lesson), and `git log -S` as a one-command negative proof that dated the wrong-citation error to the doc's authorship, not to drift | [link](./2026-09-12-scripts-readme-compact.md) |
| 2026-09-12 | API Route Tests | First-ever behavioral tests for `app/`'s route handlers (worker `api-route-tests` of `tokenmax-orch-2026-09-12`; 5 co-located files / 43 tests / 896 lines, commit `b641b31`, the first test files to live in `app/` per vitest's sanctioned `app/**/*.test.ts` include) — after a coverage census corrected the assignment's premise on the ground: the two cron routes it named were ALREADY behaviorally tested (`tests/insights.cron.test.ts`/`tests/nina.cron.test.ts` import and drive the handlers directly) and extract's `maxDuration` pinned as source text, so the five real gaps got the suites — both upload handshakes, self-described security boundaries never before executed by a test (the client route's mint-time `getUserId` gate, the shots pathname regex as path-traversal defence with prefix/ext/id-length/traversal refusals, nina-chat's own 900 KB ceiling distinct from the shots cap plus session-user binding, signed `tokenPayload` shapes, end-to-end 400 mapping through a faithful mini-SDK; the admin route's `requireAdminApi` gate BEFORE the SDK with 401-signed-out vs the pages' 404-for-non-admin, `blobEnv→gate→SDK` ordering, three pathname shapes capped by the IMPORTED constants — album 8 MB / thumb 512 KB / chat photo 2 MB — JPEG-only chat photos, the ext-vs-declared-content-type cross-check, foreign-owner refusal), the extract pair's refusal ladder and async contract (auth-first refusals that never touch the DB, non-auth failures RETHROWN not laundered into 4xx, schema issues surfaced verbatim, the `after()` callback's user/extractionId/images/invocationStartedAt payload, the 202 contract, ownership-baked-in 404 asserted indistinguishable from missing-row 404, the `Cache-Control: no-store` header the polling design rests on), and health (ok contract exposing endpoints not keys, `VERCEL_GIT_COMMIT_SHA`→`'local'` fallback, honest 500 carrying the error message on a dead DB); every suite mutation-verified — ONE load-bearing line flipped per route (chat cap→`MAX_UPLOAD_BYTES`, ext cross-check dropped, 500→200, 202→200, `isValidId` bypassed) failed exactly the test written for it then reverted; mocking pattern written down for reuse: `@vercel/blob/client`'s `handleUpload` faked thin with tests driving `onBeforeGenerateToken` at its real THREE-arg signature (incl. `multipart: boolean`, found via tsc) while the pathname predicates stay real so accepted pathnames passed production regexes; `auth/[...nextauth]` left untested by design (pure passthrough); gates green (typegen→tsc clean, full sweep 5,229/277 = +43 reconciled, eslint, prettier on the five new files only) | [link](./2026-09-12-api-route-tests.md) |
| 2026-09-12 | Nina Optional-Props YAGNI | Ran the optional-prop-vs-call-sites scan over `components/nina` — the follow-up the same-day ui-primitives-yagni session industrialized and nominated (worker `nina-optional-props` of `tokenmax-orch-2026-09-12`; the repo's largest component tree, 14,011 lines / 62 files = 31 sources + 31 co-located suites, ~3× ui, never swept for props always/never passed) via a throwaway TS-AST classifier in gitignored `.next/` enumerating all 34 inline-prop-typed components (no `*Props` types, no barrel), resolving every import repo-wide through local renames, and capturing JSX + spread attributes + hook-style call arguments — the exact `{...props}` blind spot that caught the ui session out, positive-controlled before trusting (known-alive `TypingIndicator.avatar` showed its production MessageList passer; the two providers' `children` correctly re-classified a nesting blind spot, not a finding; 13–31 spread sites per big component flagged rather than missed): census ZERO never-passed / zero always-passed+default / zero write-only props — every production site a non-spread literal JSX element, so production usage measured exactly, and the whole actionable surface was six TEST-ONLY optionals, of which commit `4f655e0` (8 files, +29/−56, all under `components/nina`) removed the four `className` ones (`NewChatButton`, `NinaAvatar`, `NinaJobElapsed` — the prop was that span's ENTIRE className and both render sites NinaJobDetail:123/NinaJobList:123 omit it, so production rendered bare all along — and `NinaJobList`) per the RunDateLink round-3 rule, each docstring recording the rule at the removal site, the two real-coverage cases rewritten onto slimmer signatures and the two "className rides" fiction cases deleted (touched suites 27→25; repo 5,186→5,184, reconciled to the digit) — while `NinaSidebar`'s `searchSlot`/`newChatSlot` stay deliberately kept as documented PHASE 3/6 seams set only by NinaSidebar.test.tsx:149 (a TAB_BAR_*-style scope/design keep, not a liveness keep) and every other candidate dismissed with verified reasons (NinaAvatar's all-3-sites-passed defaults ARE the documented phase-4 fallback face; ChatImages' documented non-interactive grid; TypingIndicator's R1 asymmetry; NinaJobList.actions genuinely sometimes; QuoteStub/NinaSidebarTrigger className production-alive 2/2 and 1/1 despite the name — per-prop verdicts, never per-prop-name); gates: typegen + `tsc --noEmit` exit 0 fully clean with zero scanner-vs-compiler disagreements (v2's spread capture retired the ui session's AppShell lesson), nina suite 311/311, full sweep 5,184/5,184 across 272 files zero flakes, eslint+prettier clean, closed loop = classifier re-run shows the four rows gone and only the two documented seams remaining (not merged — the coordinator lands; mid-session 429 → 9 idle minutes, coordinator resume ping recovered with no lost work) | [link](./2026-09-12-nina-optional-props.md) |
| 2026-09-12 | Admin Optional-Props YAGNI | Acted on the ui session's own follow-up within hours — extended the optional-prop-vs-callsite YAGNI scan to `components/admin` (73 components vs ui's 17 source files, the largest directory the scan has touched, never before asked which props are real) as a worker session of `tokenmax-orch-2026-09-12`: the TS-AST import-graph classifier rebuilt as a throwaway in gitignored `.next/` and positively controlled BEFORE any verdict (synthetic NEVER-SET/VARIATION/ALWAYS-SET probe shapes, spread-flag control, createElement attribution, ground-truth grep agreement on CircleFrame 4/4), landing commit `1fec595` (10 files, +55/−182, all under `components/admin`) — 8 dead props removed with their test cases and threading plumbing (CircleFrame `ring`+`className` 0-of-4 prod sites, `ring` born `de34b1b` never adopted by a caller in its life; ShareToNinaItem `className`+`onOpened` callerless — the R6 look converged in-component and the item left the host menu `onOpened` was to close, `cn` import gone with them; DialSlider `step` 0-of-3 → hardcoded `step={1}` and `disabled` 0-of-3 → removed through all three threading sites; PhotoReferencePicker `disabled` born `b691af8` for a save transition its only call site never armed, tiles' `disabled:opacity-50` unreachable with it), one prop made REQUIRED (CircleFrame `sizeClass` 4/4 with its `'size-24'` default never overridden — the charts sweep's showTrendLine move applied to props), `DialSliderProps` un-exported, 3 never-rendered photoIcons glyphs deleted (table 13 → 10; their test file was a suite for dead icons), and 2 doc drifts fixed with measure dates ("exactly one caller" → 3 call sites in 2 files; "ten 'use client' files" → 43 — the SAME stale "ten" the ui session fixed in its barrel the same morning, two directories copying each other's folklore) — while the session's durable additions are FOUR NEW VERIFIER LESSONS (JSX children are the `children` prop — the attribute-only scan false-flagged FolderMenu MenuItem as NEVER-SET, manual read caught it, scanner fixed, verdict flipped alive; head-cutoff greps lie AGAIN — a `head -10` inventory grep silently dropped /admin/shortcuts' two UserPicker callsites and nearly deleted `basePath` whose BOTH values are genuinely used, the AST saw all 16 sites, the repo's inventory-grep-head-cutoff lesson re-confirmed live; `Partial<Props>` test bags wall off 54 prop-rows as HAS-SPREAD(manual) with prod counts exact through the wall; data-driven tag indirection keeps AdminNavLinks' 7 icons alive at zero direct tag sites — NO-CALLSITES rows are manual-review-only) and the honest harvest (ZERO dead exports in the whole directory — the export level is clean, idea retired; ZERO clean default-never-overridden fold candidates; 12 test-only exports conditionally kept behind the tests/ fence, same geography verdict shape as ui's TAB_BAR_*; five dismissals recorded with reasons incl. FolderTree Row.menu's literal-null site and the function-called UploadQueue/useFolderUpload scan-shape artifacts); gates: typegen → `tsc --noEmit` exit 0 whole-repo (the compiler votes on every "no other setter exists" claim at once), touched+consumer suites 167/167, 443-test components/admin run with the one red proven the documented parallel-load flake signature (MediaPane latch test in a file the diff doesn't touch, 10/10 in isolation, failure set varies 3→1→0, clean-HEAD tagged-stash run 464/464 — stated honestly, no reproduction on that one run, so attribution rests on isolation+varying sets+untouched files), arithmetic reconciled to the digit (464 − 21 = 443: CircleFrame 3, ShareToNinaItem 1, DialSlider 1, PhotoReferencePicker 1, photoIcons 15 = 3 glyphs × 5 it.each), eslint+prettier clean, origin/main +11 mid-session with zero file overlap — unmerged, the coordinator lands | [link](./2026-09-12-admin-optional-props.md) |
| 2026-09-12 | Live/Integration Test Suites Audit | First-ever audit of the repo's two opt-in test tiers (`tests/live/` 5 real-API suites + `loadEnvLocal.ts`, `tests/integration/` 3 real-Postgres suites / 53 tests — never touched by a prior token-maxxing session; worker `live-tests-audit` of `tokenmax-orch-2026-09-12`) with a measured NOT-STALE verdict — actively maintained, exhaustively self-documenting, zero dead scaffolding, and the ninaImageE2E int/live pair is an intentional twin (int scripts the model decision + stubs OpenRouter; live buys exactly those two for ~$0.04), not duplication — while still landing 4 findings: the generic `test:live` script selected tests by NAME substring (`--testNamePattern=live` collected 55, only 11 real — 44 bystander unit tests across 27 files swept under the 180 s live timeout; fixed to path selection like the five per-target scripts), the "is this a real LLM_API_KEY" predicate hand-rolled in three shapes across the five live files consolidated onto exported `hasRealLlmKey()` in `loadEnvLocal.ts` (ninaImageE2E keeps its generic `realKey` for OPENROUTER/BLOB sentinels), the vision-only-era `vitest.config.ts` exclusion comment now naming both `glm-4.6v` AND `glm-5.3`, and README's unit count stamped 5,187 (measured 2026-09-12); capstone — the integration suite actually RUN against a scratch Neon DB (created per the suite's recipe, 22 migrations / 29 tables): 53/53 PASSED in 9.46 s, again 53/53 under `TZ=America/New_York` (the documented D6 proof) in 7.65 s, then dropped and verified gone, which also surfaced finding 4: both integration headers' `TEST_DATABASE_URL` runbook used a bare zsh `${UNPOOLED/\/neondb/…}` substitution whose first `/neondb` match sits inside the username `neondb_owner` — measured live producing a hostless URL that escapes the credential mask (fixed with an authority-anchored sed swap in both headers, coordinator commit `8714456`); live suites proven structurally runnable without spending money (typecheck clean; `vitest list tests/live` with `LLM_LIVE_TEST=1` collects 11/4 files with the $0.04 suite correctly gated out; default collection 5,187 tests with zero refs to either dir; `test:int` self-skips 53/450 ms); commits `1cee232` + `8fbca54`, 9 files +33/−33, comments/scripts only, no production data, zero API calls | [link](./2026-09-12-tokenmax-live-tests-audit.md) |
| 2026-09-12 | Charts Constants Consolidation | Closed ALL THREE follow-ups from the same day's charts-yagni sweep as worker `tokenmax-charts-const-consolidation` of `tokenmax-orch-2026-09-12` (12 files, +111/−39, commits `ff51184`/`50985dd`/`e9d1cfa`, zero behavior change) — where the deliverable was CORRECTING the assigned premise: `BUCKET_ORDER` is not a mergeable duplicate (load.ts's private `['10k','5k','half','full','other']` was a dominance tie-break walk; paceTrend's exported `['other','5k','10k','half','full']` is the chip-row display order its docstring says must stay; plus `TIE_PREFERENCE` as a third deliberately-separate tie-break — blindly sharing would have been a behavior bug, the twin-name trap applied to constants), so the real consolidation is of the MEMBER SET: `lib/metrics/week.ts` gains the canonical completeness-guarded `DISTANCE_BUCKETS` plus `EveryBucketListed`/`AssertTrue`, a type-only guard whose failing arm returns the missing members so the compile error names them — after the positive control caught the first draft's `never` arm passing vacuously (never is assignable to everything, so the guard guarded nothing), and with the assertion aliases EXPORTED solely because next's `no-unused-vars` (warn, no underscore pattern) would otherwise eat them; load.ts's order deleted as provably inert (threshold-disjoint buckets cannot tie on both count and metres) with the proof replacing a docstring claim that was false ("then on the fixed order" — a third tie-break that can never fire); paceTrend's two orders kept local, now `as const` + per-site guarded, so a future sixth bucket that skips a list is a compile error instead of a silently unselectable chip; plus one shared `PACE_AXIS_LABEL` config object in new `components/charts/paceAxisLabel.ts` (NOT lib/charts — the barrel's "pure data, no formatting" constitution reverses the yagni sketch's home) and the two test-only exports landed: `zoneTotalSec` deleted (false "exported so a caller can test it for zero" docstring with it), `monthWeekBucketRanges` un-exported, both tests rewritten through the public functions asserting the same numbers; gates: typegen+tsc (incl. positive controls both directions), eslint+prettier clean, f08 boundary guard (457 files), 18/18→49/49→67/67 vitest waves, DB tests deliberately skipped (the one database is production) | [link](./2026-09-12-charts-const-consolidation.md) |
| 2026-09-12 | Dead-Export Tooling (knip Adoption) | Bought the machinery every prior YAGNI session re-wrote from scratch (worker `dead-export-tooling` of `tokenmax-orch-2026-09-12`; ~14 docs since 2026-09-11 each rebuilt ad-hoc grep/TS-compiler sweeps and re-learned the same false-positive traps): evaluated and ADOPTED knip 6.35.1 as the durable dead-export/unused-file detector — `npm run knip` now re-derives the whole census from the AST import graph (`@/` alias, relative paths, through-barrel attribution, multiline imports — every trap class the hand-rolled sweeps kept re-learning), validated by a control experiment against digests of 8 prior YAGNI session docs where all six documented negative controls correctly do NOT flag (`pollDelayFor`, the review SplitsTable/ZoneBar relative-import twins, the ui barrel, `TAB_BAR_*` test-only consumers, `getInsight`, the live substring-twin `getObservedMaxHrRun`) and two correctness checks both land knip-side (`EXTRACTION_SYSTEM_PROMPT` correctly live via `lib/llm/vision.ts` — falsifying the prior llm-yagni doc's "zero TS importers" as stale; `'distillNinaMemory'` boundary-guard string literals correctly ignored as non-imports). Commit `4f4fa3f` (4 files, +983/−23): `knip.ts` config (research/** and docs/design/tokens.css ignored WITH reasons; `ignoreExportsUsedInFile: false` kept because the needless-export-keyword class is signal, not noise; entry discovery verified against the first run), `probe:f04` wired (knip's one unwired script file), dead `nanoid` direct dependency dropped (import census zero, in-repo hits prose-only, lib/id.ts reimplemented it; postcss's transitive nanoid@3 untouched), and the one documented false positive — `EXTRACTION_SHAPE`, scraped out of source text by regex by the f04 probe, invisible to any import graph — documented at the symbol and in the config instead of suppressed, with the do-not-drop-the-export warning written where the next person will look (commit `4f4fa3f`, branch `token-maxxing-2026-09-12-dead-export-tooling`, NOT merged — coordinator lands; survived an API-429 death pre-work with a clean redo). Standing census re-derived live 2026-09-12: 152 unused exports / 113 unused exported types / 2 duplicate pairs — the next YAGNI session's task list at zero setup (turnflight pair = both names live, leave alone; schema.ts alias = one-keyword fix); deliberately NOT wired into ci.yml until the backlog is burned, promotion path recorded. Gates: typecheck exit 0, eslint + prettier clean, vitest 5,183 passed + 3 = the documented MemoryTable/SelectionPane parallel-load flake (36/36 serial in both files) | [link](./2026-09-12-dead-export-tooling.md) |
| 2026-09-12 | Tools Package Hygiene | First-ever token-maxxing pass over `tools/` (the offline Python badge-art pipeline, 9 scripts / ~4,128 lines — born 2026-08-21/22 in F10/F15/F25/pwa, never touched by a prior sweep, never mapped): a full read of all nine scripts before any verdict, an exhaustive per-basename reference census across the repo with no exclusions or head cutoff plus the skill source/deploy trees outside it (`~/claude-commands`, `~/.claude/skills` — a badge tool could be referenced only from a skill), per-file git archaeology, and then EFFECT-level verification — every offline command in the directory actually run (`py_compile` ×9, `decks.py --selftest`, `badges:check` green at 22+11/style v2/one shared anchor, both decks' `gen --dry-run --all`, both promoters `--dry-run`, `gen_app_icon --all --dry-run`, the 22/22 sheet, the full control loop) — where the run-don't-read rule found the session's one real bug: the *good* control FAILED check 1 because `make_badge_control.py` still drew 1024² squares after F15 moved masters and check 1 to 1024×768, so the grader's free-fixture loop had been silently off for three weeks (nothing in CI runs controls); repaired in `9aa0d44` (+33/−13 — ground at W,H=1024,768 with the gradient over (w+h), patch `half=0.45*H` sized to the deck's MEASURED box after the first attempt's square-era `0.40*W` put the hexagon's vertices outside the frame and check 3's strips caught the sliced bone border at sd 56.4; early_bird measures 61.3%×90.2%, the rebuilt control 58.6%×86.7%; each control now fails exactly its designed check: good passes, flat → only check 3's sd floor, offcentre → only 8a, bleached → only check 3's grey band); verdict NO dead scripts (CI reads `tools/decks.json` via `badges:check`, the promoters generate the shipped `lib/{badges,records}/*-art.ts`, `icon:assets` is an npm entry, gen/extend are the sanctioned `OPENROUTER_API_KEY` users per the boundary guard, sheet/controls are documented free review tools) — what the package lacked was a map, delivered as `4db3539`: the first `tools/.workflows/package_readme.md` (234 lines: pipeline-as-data, six standing rules, per-script why + wiring, reverse wiring incl. `next.config.ts`'s immutable-header comments, gotchas); four hygiene findings deliberately NOT drive-by-fixed (sheet/extend predate `decks.py` and hardcode the badges deck, the ungated control loop, and the sibling session's inherited `RECORD_ART_SMALL_SIZE` zero-ref generator emission — recorded so it is not re-litigated) | [link](./2026-09-12-tools-package-hygiene.md) |
| 2026-09-12 | Review YAGNI Sweep | First dead-code pass over the run-review/correction flow (`lib/review` + `components/review`, 20 files / 60 exported declarations / ~4,065 lines — a core feature untouched by every prior token-maxxing sweep): built a TypeScript-compiler-API extraction sweep that parses every repo .ts/.tsx and resolves every import edge through the `@/` alias and relative paths, so verdicts come from the import graph rather than name greps — dodging the four known verifier traps (the deliberate `SplitsTable`/`ZoneBar` twins in components/ui, `lib/share/copy.ts` naming the no-longer-existent `SheetSource` in prose only, barrel re-exports reaching review, multiline import lists); found the packages in good shape and removed exactly one fully-dead symbol (`CommitReviewPayload`, a zero-reference `z.infer` mirror whose schema stays for the schema tests) plus un-exported 17 file-local symbols (code kept, `export` dropped — `actions.ts`'s 'use server' HTTP endpoints deliberately untouched), proven safe by a repo-wide `tsc --noEmit` (no hidden importer anywhere, closing the multiline/relative-import traps from the compiler side), 247/247 across the 8 review/capture/extractedSession suites, and a zero-remaining final re-sweep (commit `38ac737`, 8 files, +17/−19; no package_readme touched, no DB touched) | [link](./2026-09-12-review-yagni.md) |
| 2026-09-12 | Charts Dead-Code Sweep (YAGNI) | First-ever dead-code sweep of the charting pair (`lib/charts` + `components/charts`, worker `tokenmax-charts-yagni` of `tokenmax-orch-2026-09-12`), which certified the pair REMARKABLY CLEAN — no dead files, components, or CSS (every `charts.css` selector has an emitter; `ri-zone-1..5` come from a template literal a naive grep would call dead) — and still removed 7 pieces of never-used surface in 2 commits (`4dd4abe` lib, `c0f0cff` components; 8 files, +33/−35, zero behavior change): the barrel's `TrendWeek`/`PaceTrendLine`/`TREND_WEEKS` re-exports plus the two interfaces' `export` keywords (return types consumed by inference, named by no consumer), the never-varied `padSec`/`padBpm`/`maxLabels` params folded into commented constants (the files' own docs argue fixed §12 policy), the `weeks = TREND_WEEKS` valve on all three 12-week transforms (the shared window IS the cross-chart invariant, so the divergence parameter was speculative API), `ChartFrame`'s never-passed `tableSummary`/`className` props, `LegendKey`'s dead `'dashed'` variant, and `showTrendLine` promoted to required (its only caller always passes the §9 gate); two production-dead exports (`zoneTotalSec`, `monthWeekBucketRanges` — each imported only by its test file) verified and scope-blocked rather than touched, with every would-be caller located by file:line; three look-dead things investigated and kept (`PaceHrPoint.timeSec` read by SplitsTable:154, twin `BUCKET_ORDER` in load.ts, unrelated Python/OG `ZONES`); gates: 45-symbol word-boundary sweep with positive control, typegen+tsc+eslint+prettier clean, f08 boundary guard (449 files), 116/116 consumer tests (DB tests deliberately skipped — the one database is production) | [link](./2026-09-12-tokenmax-charts-yagni.md) |
| 2026-09-12 | Lib LLM YAGNI Audit | Closed the last hole in the two-day YAGNI campaign — `lib/llm` (3,249 lines), the largest lib package never swept while lib/db, lib/admin and lib/nina all got the pass — as a worker session of `tokenmax-orch-2026-09-12`: the public export surface cut **93 → 57 symbols (−39%**; facts.ts alone 27 → 14, prompts/narrate 10 → 4, narrate 13 → 7) via 6 exports deleted outright (the zero-importer `canonicalizeForHash` re-export, `coerceNarrativeTextModel` + `NARRATIVE_TEXT_MODEL_DEFAULT` whose only reference was a docstring's false claim, the `Observation`/`Verdict` type twins InsightCard declares locally, the `promptVersionFor` re-export whose feeder import existed only to feed it) and 30 internal-only unexports (13 facts structural types, narrate's store/deps/result types, vision's `callVisionWithFetch`, the six prompt consts) — every removal verified against the four verifier traps, which caught four real twin near-misses live (badges' unrelated `SessionFacts`, InsightCard's local `Verdict`, the `MIN_REPAIR_BUDGET_MS` pair in narrate.ts and lib/extract, nina's `productionDeps`) — while the sweep's real value was the stale-claim catch: catalog.ts's docstring claimed a test pin that did not exist, so the pin was *written* (`NARRATIVE_TEXT_MODEL_IDS` must contain `env.LLM_MODEL` — the degrade target stays inside the dropdown's vocabulary) instead of the claim deleted; `EXTRACTION_SYSTEM_PROMPT`/`EXTRACTION_SHAPE` deliberately keep `export` with zero TS importers because scripts/f04-e2e-probe.mjs scrapes them by the literal regex `` export const ${name} = ` `` — a non-TS consumer contract tsc cannot see; 13 files, +53/−70, commit `5421e6b`, gates green incl. the serial full sweep (265 files/5,093 tests) and the 9-symbol payload-boundary guard; left open: whether `ci:llm-payload-guard` is actually wired into any CI, and narrate.ts's unmeasured week/month budgets | [link](./2026-09-12-llm-yagni-audit.md) |
| 2026-09-12 | Auth/Push/Runs/Trends YAGNI Sweep | Dead-code hunt across the seven never-swept small packages (`lib/auth`, `components/auth`, `lib/push`, `components/push`, `lib/runs`, `components/runs`, `components/trends` — 22 files, ~2,045 lines, commit `04e79fe`, +13/−64): all ~60 exports censused by full-file reads plus repo-wide word-boundary greps with line-level classification of every ambiguous hit (four were comment mentions, not consumers — counting them as refs would have blocked the sweep); removed `decodeNinaPushPayload` + its 3 self-referential tests (a TS twin of the service worker's defensive reader that the worker cannot import — consumed only by its own test), un-exported 8 internal-only symbols (`PUSH_TITLE`, `PUSH_TARGET_URL`, `PUSH_NOTIFICATION_TAG`, `TERMINAL_PUSH_STATUS_CODES`, `pushSubscriptionSchema`, `PushActionResult`, `PushSendReport`, `sendPushToSubscription`) with the post-edit repo-wide `tsc --noEmit` as compile-time proof of zero importers, and dropped `SignOutButton`'s never-passed `fullWidth` prop; the honest headline is the verified-alive ledger — the `lastSuccessAt`/`expirationTime` deliberate omissions, the network-error retry branch, the CJK truncation guard, and the 20+-site `now:` injection convention a naive sweep would have deleted, now written down with reasons; gates: typegen + tsc clean, vitest 5,090/5,090 serial (the 1-then-3 parallel reds reproduced as the known MemoryTable/SelectionPane flake on untouched files), eslint + prettier clean; zero package_readme edits, zero DB access, worker session — coordinator lands the branch | [link](./2026-09-12-auth-push-runs-trends-yagni.md) |
| 2026-09-12 | Profile & Share YAGNI Dead-Code Audit | Closed the corner every prior YAGNI session routed around (worker `profile-share-yagni` of `tokenmax-orch-2026-09-12`): a full dead-code audit of `lib/profile` + `components/profile` + `lib/share` + `components/share` — 17 files / 2,506 lines, 68 exported symbols word-boundary-grepped then upgraded to import-graph analysis after two twin-name false leads (`avgHrPctMax`'s unrelated same-named local in `lib/metrics/session.ts` + field-name noise; `shareOrigin`'s 11 refs proven real) — found the estate tight (zero dead files/helpers/branches, 22/22 copy constants consumed, every `SharedRunView` field render-traced on `s/[token]`) with exactly one real find: 7 exported types imported by nobody (all callers pass structural literals) de-exported in one commit (`8527ba8`, 4 files, 7+/7−, types kept — load-bearing locally), plus four leave-and-note follow-ups (`profileWriteSchema` and `avgHrPctMax` test-only behind the `tests/`-edit constraint, the two `SHARE_SHOWS_*` constants as deliberate test-pinned decision docs, and the ShareButton/ShareLinkPanel duplicate select-on-focus link input flagged as consolidation, not death); gates: typegen+tsc+eslint clean, six covering suites 122/122, no package_readme exists in any of the four dirs (full-suite reds are the known parallel-load flake — varying across two runs, pass in isolation; unmerged, the coordinator lands) | [link](./2026-09-12-profile-share-yagni.md) |
| 2026-09-12 | Ingest-Pipeline Dead-Code Sweep (Extract/Photos) | Swept the ingest pipeline (`lib/extract`, `components/extract`, `lib/photos` — the three packages no prior YAGNI pass had dedicated) as a full census rather than a grep-and-pray: all 20 files / 2,343 lines read end to end, every exported symbol (~60) word-boundary-grepped across `app/`, `lib/`, `components/`, `scripts/`, `tests/`, `research/`, with all four known verifier traps applied (twin names found and dismissed — two different `KindHolder` interfaces, `EXPLORER_UPLOAD_CONCURRENCY` as a different constant, narrate's own `MIN_REPAIR_BUDGET_MS`; one barrel re-export noted out of scope; both file-path-string test suites' assertions checked before any edit; positive controls green with `tsc` as arbiter) — net 11 files, +13/−36, two commits (`b2a8f11`, `a099686`): three deletions (`UPLOAD_CONCURRENCY`, whose "two at a time" docstring was factually false — the picker uploads unbounded-concurrent; `shortEdgeOf`, whose only caller was its own test and whose docstring claimed a nonexistent QA consumer; write-only `Tile.originalBytes`), 8 types un-exported (all still private load-bearing shapes), both compressors' never-populated `opts` bags dropped including an unreachable abort rethrow — while the honest headline is the NEGATIVE result: after the full census these never-swept packages are almost entirely alive (~2,300 lines carrying only ~36 dead ones), and the session's durable value is the verdict table (every export → its consumers) plus the recorded KEEPS (test-seam exports per repo convention, `noUncheckedIndexedAccess` totality guards, `KNOWN_EXTENSIONS` entries pinned by `save.test.ts`) so the next sweep doesn't re-litigate; two out-of-scope ghosts flagged not fixed (a leftover mock key in `Composer.test.tsx`, a plan file quoting the old exported `ContentHashInput` signature); gates green (typegen, `tsc` exit 0, 265 files/5,093 tests, prettier clean on all 11 edited files; zero DB access, no package_readme touched) | [link](./2026-09-12-extract-photos-yagni.md) |
| 2026-09-12 | UI Primitives YAGNI | Gave `components/ui` the dead-code pass it had never had (worker `ui-primitives-yagni` of `tokenmax-orch-2026-09-12`; the repo's largest component tree, 5,234 lines, made safe by the prior day's 205-test coverage sweep): all 17 source files / 44 exports classified by a throwaway TS-compiler-AST import-graph script (kept out of git) built to defeat the four known verifier traps — twin names resolved per module, relative imports resolved, barrel re-exports mapped transitively through `index.ts`, multiline imports read as AST — positive-controlled before trusting (synthetic probe read DEAD, through-barrel attribution verified), landing commit `dba8f17` (9 files, +25/−46, all under `components/ui`): 3 zero-importer internal types un-exported (`AppShellScreen`, `SheetProps`, `PanelParam`), 3 speculative/test-only `className` props deleted per the RunDateLink round-3 rule ("a prop with no caller is a second way to render, waiting") — `AppShell`'s set at 0 of 11 call sites and caught by `tsc` after the JSX-attribute scanner missed a `{...props}` spread in its own test, `EmptyState`'s and `Field`'s set only by their own tests, cases removed (suite 5,093 → 5,091, reconciled to the digit; `EmptyState`'s `cn` import fell with it) — the `TAB_BAR_*` test seams deliberately KEPT on scope grounds (consumer `tests/tabbar.geometry.test.ts` lives outside the directory fence), the barrel's claims verified rather than trusted (15/15 re-exports pulled through, `Toast` confirmed still nonexistent), 3 doc-claim drifts inside the directory measured and fixed with their 2026-09-12 measure dates ("ten client components" → 32; AppShell "five pages" → 9 pages + layout + 2 loadings; Sheet "two call sites" → 3 named), and 2 false alarms dismissed with reasons (`NumberInput.ref` inherited via shared `InputProps`; `buttonClasses` is a function, not JSX) — the honest verdict being that the tree was already unusually clean: 3 un-exports, 3 speculative props, 3 stale counts, verified-true everywhere else (tsc clean excl. known typegen noise; components/ui 203/203; full sweep 5,089 + 2 pre-existing MemoryTable parallel-load flakes reproduced on clean HEAD via stash, 20/20 serially; eslint+prettier clean) | [link](./2026-09-12-ui-primitives-yagni.md) |
| 2026-09-12 | Badges/Records YAGNI Sweep | Worker session of `tokenmax-orch-2026-09-12`: swept `lib/badges` + `lib/records` (the never-audited gamification area) via a whole-repo liveness map of all 82 exports + every private top-level declaration, classified self-file/cross-package/production/test/script/guard — deleting both zero-importer barrels (`lib/badges/index.ts` 69 lines, `lib/records/index.ts` 24 lines; every consumer imports a direct submodule path, proven by exact-quote + dynamic-import + `export *` + alias greps, the only remaining matches being SQL string literals `delete from "records"`) and un-exporting the three barrel-orphaned types (`CommitBadgeOptions`, `RecordDirection`, `RecordUnit`) — one commit `f903ef4`, 4 files, +3/−96 — while the harvest is the recorded NON-removals: zero-ref `RECORD_ART_SMALL_SIZE` is generator output (`tools/make_badge_assets.py:311` emits it per deck; fix belongs there), the records deck's 11 `.sm.webp` derivatives ship unrendered per the still-open F25 pre-generation bet, three test-only exports (`isBadgeKey`/`previousIsoWeek`/`badgesForRun` — with the asymmetry that records twin `isRecordKey` has 6 production refs) kept because tests are out of scope, and the decks.json/check-badge-art.mjs anchored names left untouched; a counter-key bug (`pkg` vs `pkgx`) in the second liveness pass that silently zeroed cross-file refs was caught by pass-disagreement + direct grep before it flagged live types for deletion (265 files / 5,093 tests green) | [link](./2026-09-12-badges-records-yagni.md) |
| 2026-09-12 | Insights/Metrics/Panel Dead-Exports Sweep | Closed the dashboard layer the 2026-09-11 dead-code campaign never reached (worker of `tokenmax-orch-2026-09-12`; strictly `lib/insights`, `components/insights`, `lib/metrics`, `lib/panel` — 16 files / 2,123 lines, no package_readme, no DB): a TypeScript compiler-API import-graph inventory over all 705 repo modules (resolved named/default/namespace imports, barrel re-export edges, same-file usage, dynamic refs) found 10 zero-importer exports and un-exported every one with its in-file uses kept — `readInsightPayload`, `EnsureInsightResult`, `dominantBucket`, `getPreviousInsight`, `FlagSeverity`, `PaceComparison`, `MonthMetrics`, `RecoveryInput`, `WeekMetrics`, `PanelKind` (+ its stale "record arrives with #25" comment fixed; shipped) + the 5 barrel lines advertising them in `lib/metrics/index.ts` (commit `7f07c41`, 9 files, +11/−16) — where the session's real trophy is that the verifier caught a bug in ITSELF: the first barrel BFS hopped forward through the module's own re-exports instead of backward through files re-exporting FROM it, missing every barrel-mediated caller and false-flagging live `ACWR_OUT_OF_RANGE`/`VOLUME_JUMP`/`Acwr` as dead; the word-boundary grep cross-check caught the discrepancy before any edit, the BFS was fixed to reverse edges, and a positive control (scratch `ZZZ_DEAD_SCRATCH` planted behind both a direct export and a barrel line, both entries flagged) proved the fix — a clean-tree `--noUnusedLocals` baseline already silent on all four dirs proving the debt was all in the export surface, not inside files; gates green (typegen+tsc, eslint zero findings in the four dirs with 4 stash-proven pre-existing errors elsewhere, 265 files/5,093 tests, zero dead exports on post-edit re-inventory; `next build` skipped — symlinked node_modules vs Turbopack, known artifact) while the unremovable-in-scope findings are recorded for a future `tests/`-scoped session: the `hrMax` transition trio (`hrMaxTransitionAt`/`resolveHrMaxAsOf`/`HrMaxTransition`) with ZERO production callers — the F06 §4.5 banner planned and documented but never shipped, kept alive only by two test suites, its own doc comment falsely claiming "F06's run detail page calls it once per render" — and `WeekRunSummary`/`MonthRunSummary`, test-fixture-only | [link](./2026-09-12-insights-metrics-panel-yagni.md) |
| 2026-09-12 | Schema Misc Todos Archive | Two-stream worker session (coordinator `tokenmax-orch-2026-09-12`, slug `schema-misc-todos-archive`) that closed the digest's "only 3 of 7 todos.md archived" flag and extended the dead-code campaign to four unswept lib dirs: a 51-export census across lib/schema, lib/derived, lib/date, lib/flags found 3 exports dead outright — `emptyExtractedSession` whose docstring claimed an F05 consumer that never existed (ReviewClient renders failed via `lib/review/draft.ts`), self-unused `sectionForField` (test re-anchored to `FIELD_SOURCES`), and the `ExtractRequest` type — plus 8 self-only symbols un-exported since TS has no package-private and one dead `export { SCREEN_KINDS }` re-export hop cut, with lib/date proven fully live and a recorded keep list (test seams, out-of-scope test consumers, the write-only `changedFieldPaths` whose writer sits outside the assignment), every consumer's full import block verified multiline-aware against the memory trap list and tsc as arbiter (commit `f332d1a`, 5 files, +33/−66); then the todos-archival trilogy completed by archiving all 19 completed tasks across the four remaining ledgers (scripts 2, lib/db 7, lib/admin 4, components/admin 6) with closure derived from git, not the files' own bodies — lib/db's P1-DB-A002 listed pending while its own header said 0 active, proven completed by `7fb7f90` so the real count is 7 not 5, lib/admin 4 not 3, and scripts' withheld production `--apply` closed by measured apply runs (48/52 hashed, 17 repointed) — open obligations (deploy-time migration verifications, ten manual browser checks) kept as suffixes, 19/19 plan paths resolving package-relative (commit `a1db572`, 4 files, +86/−368; typegen+tsc+228 targeted tests green, full-suite reds the known MemoryTable load flake; not merged — Worker Mode reports to the coordinator) | [link](./2026-09-12-schema-misc-todos-archive.md) |
| 2026-09-12 | Docs Nina Persona Audit | Audited `docs/nina/persona.md` (29.8KB prose "canon") claim-by-claim against the implementation it describes (`lib/nina/persona.ts`, `tuning.ts`, `prompts/*`, schema/`drizzle/`, `app/admin/*`, git log) and landed ten corrections in one commit (`69fe78c`, 1 file, +51/−37) — the headline being a whole missing persona (the Instructor: sixth relationship level in `NINA_RELATIONSHIPS`/`NINA_ADDRESS`/`NINA_RELATIONSHIP_BLOCKS` with the gated `INSTRUCTOR_COACHING` block, absent from a doc that said "Five levels"), plus the body repeal corrected to four traits from three in two places, `horny` added to the trait defaults, the three appearance paragraphs restored to verbatim copies of `NINA_BODY_SENTENCES`/`NINA_FACE`/`NINA_DEFAULT_OUTFIT` (script-verified exact match, so future drift is diffable), the verbosity dial's row rewritten to the mechanism that shipped after a *declined* proposal, rotted pinned version constants unpinned, the purged `nina_turns` revision row removed, and the status line promoted draft → living canon — while the doc's spine (voice examples, anger ladder bands, trait/dial tables, defaults contract, twelve F34 repeal sites) verified accurate | [link](./2026-09-12-docs-nina-persona-audit.md) |
| 2026-09-12 | Lib Admin Pkg-Readme Compaction | Compacted `lib/admin/.workflows/package_readme.md` 1,300 → 890 lines (−32%; one commit `e9d2dc3`, 651+/1,061−, markdown-only) by rebuilding it from a verification base instead of trimming it: mechanical export extraction across all 24 `lib/admin` modules, a repo-wide import census of every `@/lib/admin/*` specifier, and targeted reads of every documented module — zero claims restated on trust. The file had missed ~72 commits since 2026-09-09, so stale material ran deep: the 8-action memory era replaced by the real 4 actions + `memoryDeleteSchema` union (its "append comes first" invariant dropped as folklore per the code's own header), the deleted `resetNinaTuningAction` and `/admin/photos` route removed, chat-photo actions 4 → 6 and album actions 7 → 15 with the write-time dedupe and chat-photo adoption documented for the first time, `avatarRegisterSchema`'s "no live caller" annotation corrected (`explorer/thumbnail.ts` imports it), and the 4 stale export lines the 2026-09-11 dead-exports sweep left behind fixed — while coverage ADDED six undocumented modules (`folderOps`, `imageGenTestView`, `textModelActions`, `shareToNina`, imagegen map rows, the `?view=media` switch); the ~160-line inline per-task changelog became one Recent Changes entry pointing at `git log -- lib/admin`, killing the recurring-context cost the idea named | [link](./2026-09-12-pkg-readme-lib-admin.md) |
| 2026-09-12 | Nina package_readme Compaction | First 2026-09-12 worker session (`tokenmax-orch-2026-09-12`, slug `pkg-readme-nina-cmp`): compacted the largest bloated package readme — `components/nina/.workflows/package_readme.md`, loaded into context on every nina task — from 1,017 lines / 96.7 KB to 713 lines / 61.5 KB (−36%, commit `11f5689`, 514+/818−), but only after a claim-by-claim verification sweep (dated `git log` on `components/nina` + `lib/nina` since 2026-09-10, per-commit diffs ×7, greps of every named export/prop/constant/suite/boundary claim) found ~15 claims the 2026-09-11 work had falsified or drifted — the "node cannot render a client component so no component tests" story (31/32 files now carry happy-dom suites), the retired 90 s `NINA_TURN_STALE_MS` give-up identity (now `NINA_BACKGROUND_BUDGET_MS` 240 s), the gone Prompt section in NinaJobDetail, ChatChrome's vanished 32 px disc exception, the "media-dedupe phase 2 of 4" note (4/4 complete), and a `lucide-static` dependency that does not exist among them — while the full skill skeleton (Overview, 32-file module map, turn/keyboard/dedup/about/sidebar sections, export table, deps, concurrency, errors, performance, usage+gotchas) survived and the duplicated double changelog (~85 lines) became one per-task "Recent changes" record | [link](./2026-09-12-pkg-readme-nina-cmp.md) |
| 2026-09-12 | Lib Nina Todos Archive | Completed the package-ledger archival trilogy: `lib/nina/.workflows/todos.md` — the one ledger yesterday's root/nina-component archive sessions left untouched (703 lines vs siblings of 53–157) — compressed 703 → 90 lines by archiving all 35 completed tasks as one-line entries (TaskID, title, plan set + phase, bracketed plan path; all 35 paths verified to resolve on disk across the two bases — 25 package-relative adopted copies + 10 repo-root-relative source plans, the 10 adopted copies proven never to have existed rather than lost), with the 3 still-open Open/Outstanding obligations kept verbatim as line suffixes and the one live task (P1-NIN-A018) byte-identical; zero dedup needed (36/36 unique IDs at the per-entry uniq -c gate) and Quick Stats verified honest before the rewrite (Completed: 35 was the real count, unlike the root session's stale 37); plus a tooling correction recorded: `todos.py plan-of` is a pure ID→path deriver that exits 0 for any canonical ID whether or not the file exists, so on-disk resolution is the only real reachability receipt (1 file, +45/−658, commit `d81aa7f`) | [link](./2026-09-12-todos-nina-lib-archive.md) |
| 2026-09-12 | Lib Nina Package Readme Compaction | Compacted the repo's worst-bloated package doc — `lib/nina/.workflows/package_readme.md` 3,767 → 557 lines (−85%, ~6.2 lines/file vs scripts' 8.4 despite a ~90-file package) — to yesterday's scripts calibration: ~2,700 lines of per-phase implementation narratives (accreted after every set since 2026-09-05, duplicating `.workflows/plan/` and these session docs) cut, and every surviving claim re-derived from the tree with ~40 exhaustive greps (no head cutoffs), which caught the stale ones mid-rewrite — both "no importer" claims (tuning-aware exports, `captionNinaPhoto`) falsified as wired long ago, tuning/shortcut reads re-anchored from actions.ts into turnrun.ts STEP 2's four-way Promise.all, image budgets corrected (anchored timeout 220s→235s, run budget 240s→255s, plus 150s unanchored/290s worker), "six generations a day" replaced with `NINA_IMAGE_DAILY_CAP` fallback 30 (env-tunable via `ninaImageDailyCap()`, clamped 1..200, counting failures), `maxDuration=300` re-censused to four segments, soft-delete predicate re-censused to ten WHEREs across nine functions, and the per-migration applied/not-applied table dropped entirely because it rots per deploy (the rule stays) — with every volatile count stamped with its 2026-09-12 measure date (1 file, +516/−3,726, commit `0b7020e`) | [link](./2026-09-12-pkg-readme-nina-lib.md) |
| 2026-09-12 | Docs-Design Staleness Audit & Compaction | Continued yesterday's dead-links session into the content half: every checkable claim in `docs/design/DESIGN_INTEGRATION.md`, `docs/design-brief.md` and `docs/design/tokens.css` verified against the tree before editing — the token mirror came back TRUE for all 25 tokens in both schemes (the 4 radii match by value under `--radius-*` literals, a naming wrinkle now documented at the trap site after the audit's own naive name-keyed diff skipped them) — then 9 stale claims fixed (the brief's bare retired-record citation on line 4, the `list_projects` re-pull trap, "Thresholds are fixed" vs `lib/charts/zones.ts`'s per-run HRmax bounds, R-32's threshold location since R-42, the 4-tab bar that ships as five with Nina, the ten records that ship as eleven, `--miss` now consumed as the zone fallback, a "Still open" heading holding only closed items, plus a shipped-deltas landing note) and 3 duplications compacted (+39/−30, comment-only in tokens.css) — with both brief edits above line 175 kept line-count-neutral because `components/admin/DialSlider.tsx:64` cites that line by number | [link](./2026-09-12-docs-design-audit.md) |
| 2026-09-12 | Root Package Readme Compaction | First-ever compaction of the root `.workflows/package_readme.md` (1359 → 769 lines, −43%; +422/−1012, commit `f6e595a`) — twelve per-phase "Recent changes" entries (2026-09-05..09-11, roughly half the file) folded into one rolling summary under a new written one-paragraph-per-wave policy (durable statements in the body sections and the constants' docstrings, narrative in git history) — with every surviving claim re-verified against the current tree, finding major drift the doc had never recorded: geometry re-based to the compact bar (`TAB_BAR_HEIGHT_PX` 58→39, outer 40, new `TAB_BAR_CONTENT_DROP_CSS`, `COMPOSER_RESTING_PX` 68→60, `BOTTOM_GAP.chat` 7.5rem→7rem = 60+8+32+12, FALLBACK 100; R5/`18b0c58` named), the safe-area inset actually INSIDE `composerBottomCss`'s gate (the doc asserted the opposite) with three new exports (`composerPadBottomCss`/`panelBottomCss`/`attachStripPadBottomCss`) documented, bar state re-homed to `AppShell`'s `NinaBarProvider` with the sidebar rail's "up" dispatching toggle (`listScrollRef`/`onScrollToTop` gone), the route table fixed in both directions (+`/nina/jobs` and `[id]`, −merged-away `/admin/photos`, `/nina` maxDuration 300 not 60, 19 pages not 16), counts re-measured (22 lib dirs not 23, 13 components dirs not 15, ~270 suites not ~100 with ~74 happy-dom — the harness postdates the doc's "cannot be asserted at all" claims, which were reworded honestly rather than deleted), the install contract updated for `ADMIN_PWA_ICONS` + `admin/apple-icon.png` (the same-art known-cost note gone) with the viewport merge now cited from Next's `mergeViewport`, and the retired `RECONCILIATION_v0.1.0.md`/`ROADMAP_v0.1.0.md` citations repointed at git history (prettier --check passes; docs-only diff — root worker of the day's six-readme fan-out, scripts/ left alone at 226 lines) | [link](./2026-09-12-pkg-readme-root.md) |
| 2026-09-12 | Docs Architecture & README Refresh | Closed the drift window the 2026-09-11 architecture synthesis opened the same day it predicted drift (worker `docs-architecture-readme-refresh` of `tokenmax-orch-2026-09-12`): extracted the exact removal checklist from the 9 code commits after the docs' baseline `e0add93` (7 removed functions, 28 de-exported symbols), falsified the assignment's named suspect (`6fe5f82` predates the baseline), and fixed every real hit — architecture.md's F03/§6 rewritten around the surviving `getObservedMaxHrRun`, §4.2 invariant 1 naming the allow-list the guard actually enforces (`isUniqueViolation`, not the phantom `requireAdmin()` member), §8's dead `/admin/photos` route struck (pre-existing error, `746e454` purged it before the doc existed) — plus README's 3,566 → 5,093 tests (full suite re-run green today), "six a day" → `NINA_IMAGE_DAILY_CAP`, "Seven tabs" → six, and all three v0.2.0 mentions → v1.0.0 (3 files, +43/−29; 70/70 cited paths exist, stale-string sweep clean) | [link](./2026-09-12-docs-architecture-readme-refresh.md) |
| 2026-09-12 | Admin Package-Readme Compact | Opened the package-readme compaction campaign with `components/admin` (worker session, coordinator `tokenmax-orch-2026-09-12`): 1,950 → 1,291 lines (−34%, +1,144/−1,803, one commit) while ADDING three sections that didn't exist (The text-model select, `/admin/image-generation`, top-level Gotchas) — where the real value was the stale-claim harvest the verify-then-compact order forced, ~15 facts fixed, headlined by: the module map naming two files the /admin/memory one-table rebuild deleted (`MemoryLedger`/`MemorySlots`) and omitting their replacements (`MemoryTable`/`TextModelSelect`); the central "no test files and why that is correct rather than a gap" claim falsified by vitest's own include pattern and 27 colocated happy-dom suites (rewritten as a three-layer testing story); the "migration 0003 applied to no live database" 2026-09-04 snapshot replaced with a measured read-only production count (20/20 migrations, `nina_avatars` 36 rows, `nina_folders` 0); ImageGenPanel's pre-template section brought current (editable prompt template `0b85fcc`, model select `1e9b4d5`, ✕ clears `94fdb1a`, test panel deriving from `lib/admin/imageGenTestView.ts`); and the `/admin/nina` "Image collection" nav rename reflected — with every test-enforced guard compressed-but-kept and the load-bearing constants claim-by-claim grep-verified (`NINA_CHAT_PHOTO_PAGE_SIZE=48`, `ADMIN_LEDGER_PAGE=200`, `ADMIN_FOLDER_OP_MAX_IDS=500`, `NINA_TRAITS=12`, `NINA_DIALS=4`, …); pattern written down for the other six readmes (lib/nina's is 3,767 lines): measure, don't extend, and stamp volatile numbers with their measure date | [link](./2026-09-12-pkg-readme-admin-cmp.md) |
| 2026-09-12 | Lib DB Package Readme Compaction & Re-verify | Compacted `lib/db/.workflows/package_readme.md` 1,098 → 657 lines (−40% of the file every persistence-layer task loads as recurring context; ~520 lines of per-phase narratives became a ~20-row documentation-history table), where the real value was the verification harvest — every claim re-derived rather than copy-edited, yielding a deleted type still in the Exit list (`MonthlyTotal`), a whole table missing from the "complete" inventory (`appSettings`/0020), a module-private union listed as exported (`AdapterAccountType`), db-instance importer counts corrected to measured 60 source / 72 static / 90 incl. dynamic (the "five files" claim → ten, via the Nina turn pipeline's direct imports; first-pass line-grep 76/62 was inflated by comment quotes, settled by specifier-exact parsing), the scoping invariant's "exactly one exception" fixed to two (`listActiveUserIds`), the cascade gotcha rewritten around 0013's `set null`, retired ROADMAP/RECONCILIATION pointers repointed at the ten live R-x rulings, and migration names realigned to the live journal — capped by a read-only production measurement that found the one true deploy defect the file never mentioned: `0011_rare_blockbuster` (DROP `nina_memory_facts.confidence`) is unapplied AND watermark-skipped, so `db:migrate` skips it silently forever (column inert in prod; repair shape recorded: hand DROP + constructed journal fix, memory updated); plus `schema.ts`/`queries.ts` header comments aligned so source and readme state the same facts — an adversarial reviewer subagent's independent re-derivation caught seven further discrepancies (four substantive) before the single commit `356c5f4` (3 files, +186/−624; typegen+tsc+prettier green) | [link](./2026-09-12-pkg-readme-lib-db.md) |
| 2026-09-11 | Docs-Design Dead-Link Repair | Repaired the one place the 204fd34 retirement of the v0.1.0 contract trio missed: `docs/design/DESIGN_INTEGRATION.md`'s dead citations of the retired `RECONCILIATION_v0.1.0.md`/`ROADMAP_v0.1.0.md` now resolve via a "Where the retired citations point" note (exact `git show 204fd34^:` retrieval), with the banner re-pointed at the live token sheet + the shipped components carrying R-41–R-46, Roadmap §4.6/§4.7 re-pointed at `lib/badges/*` and the generate-badge style contract, and two stale "Still open" items closed — keeping the doc's full record text, since it is the canonical home of rulings R-29–R-35 (the record's Part IV delegates them here) plus a comment-only fix to `docs/design/tokens.css`'s header (2 files, +48/−18; 17/17 cited paths resolve) | [link](./2026-09-11-docs-design-dead-links.md) |
| 2026-09-11 | Lib Nina Queries YAGNI | Finally acted on the most-deferred cleanup in token-maxxing history (flagged 4–5 prior sessions, each deferred for missing test coverage — coverage that landed earlier the same day): a genuine unused-export audit of all 114 `lib/nina/queries.ts` exports (84 functions + 30 interfaces, word-boundary-grep'd repo-wide after proving no namespace/barrel imports exist) found exactly ONE dead export — removed `countNinaSessionMessages` (23 deletions), deliberately superseding task #136's recorded keep-decision — plus refiled the 5 `[x]`-completed tasks misfiled in `lib/nina/.workflows/todos.md`'s Active section into Completed with two timestamps repaired from commit archaeology (typecheck clean; full suite 265 files/5,107 tests green) | [link](./2026-09-11-lib-nina-queries-yagni.md) |
| 2026-09-11 | Scripts Package Hygiene | The day's only scripts/-scoped worker session — a 2-file, +244/−17 doc diff that replaced two pieces of silently-wrong bookkeeping with measured facts: reconstructed P1-SC-A000's abandoned closure from git (root cause: /implement Step 3 skipped by design in `aee6b75`, landing `62727a2` never wrote the block) and measured the outcome instead of minting a moot "run --apply" follow-up (read-only: `nina_shortcuts` 25 + `nina_memory_facts` 3 = the original 28 conserved, migration already complete), and expanded `package_readme.md` 7 → 226 lines into a real map of all 27 scripts in five groups with the directory's six standing rules — catching en route that the old readme's central warning ("dedupe --apply has NOT yet been run") was stale per `c2c2ca5` and the live DB (48/52 hashed, 17 repointed) | [link](./2026-09-11-scripts-package-hygiene.md) |
| 2026-09-11 | Nina Todos TaskID De-dup & Archive | Fixed the package's one real TaskID collision at its root: two parallel 2026-09-09 plan sets each minted `P1-CN-A001` and their adopted copies add/add'd on the same ID-derived path, so for two days the keyboard task's Plan pointer described the *window-pin's* plan — the window pin re-minted `P1-CN-A005` (Former ID stamped, copy `git mv`'d, `Depends on` re-pointed), the keyboard copy restored byte-exact from `a1bed18`, all four living `package_readme.md` references corrected, and a tooling blind spot recorded: `todos.py validate` exits 0 on a same-file ID collision (`tasks()` calls the repeat a "log echo"), so the binding gate is a per-entry `grep \| sort \| uniq -c`; plus all six completed tasks archived to single lines (the two live on-device Outstanding checklists kept), Active Tasks/Completed view scannable at 0 active | [link](./2026-09-11-todos-nina-archive.md) |
| 2026-09-11 | Plans Archive | Gave `docs/plans/` the archival mechanism it lacked (worker session under the day's orchestrator): all 36 SHIPPED/SHIPPED+AMENDED plans — 19,224 lines, proven byte-preserved as 36 pure 100% renames — moved to `docs/plans/archive/` behind a new charter README, the 3-week-old F16 filename collision settled by renumbering the race's second claimer to `F16b-upload-kind-swap.md` (evidence-dated 23 minutes after the first), and ~30 live plan-path reference strings in code (source/test/config comments, 4 CI guards' error messages, 6 badge-art tool headers) plus all living docs repointed at the archive — path substrings only, zero behavior change; flagged that main's `format:check` CI gate is already red with ~48 inherited dirty files | [link](./2026-09-11-docs-plans-archive.md) |
| 2026-09-11 | Lib Admin Dead-Exports Audit | First dead-code pass over `lib/admin`'s two largest files (`filetree.ts` 1,151 ln, `ninaAlbumActions.ts` 1,224 ln), run as a worker session under the day's parallel coordinator: a name-grep-refusing census (74 exports enumerated, every import site extracted symbol-by-symbol across ~20 files, every grep hit manually classified code-vs-prose) found the dead surface is type-only — 15 type exports named by zero importers whose only external hits were comment prose — unexported in two 1-file commits (6d9d6f8, 422daa5) totalling 15 keyword-only changed lines; all 15 Server Actions, all 20 functions and all 13 constants proven live, test-only callers kept, gates green (typegen+tsc+eslint, 51 files/1,128 tests serially) | [link](./2026-09-11-lib-admin-dead-exports.md) |
| 2026-09-11 | Todos Root Archive & TaskID Dedupe | First worker-mode session for the coordinator fan-out: shrank the root `.workflows/todos.md` 844 → 84 lines by archiving all 41 completed tasks into its previously-empty Archive section (one line each — TaskID, title, plan set + phase, bracketed plan path; all 40 paths verified to exist; stale Quick Stats "Completed 37" corrected to 41) and deduped the TaskIDs a 2026-09-09 mint race had double-assigned across plan sets — the SIMPLIFY pair renamed to verified-unspent A040/A041 (highest+1 + `git log --all -S`), plans `git mv`'d as R100, 9 readme sites re-pointed by content; final audit: A040/A041 at exactly 11 sites, A025/A026 at exactly 4, all legitimately SEARCH's | [link](./2026-09-11-todos-root-archive.md) |
| 2026-09-11 | Components Dead-Code Sweep | The day's first YAGNI/dead-code session (worker of `tokenmax-orch-2026-09-11`): swept all 27 exported symbols across the 17 files of `components/{extract,auth,push,runs,insights}` + `lib/derived/` via repo-wide word-boundary greps (barrel re-exports and `next/dynamic` string refs: none) and full-file reads to separate real callers from prose mentions — every one of the 17 files is alive, and exactly 4 export keywords had zero importers repo-wide (`ExtractionStatusState`, `RunListRow`, `WeekDivider`, `PushSetupFallback`), each un-exported with nothing deleted, while 4 more zero-importer symbols were kept as documented contract-test seams; plus a stale "currently-no-op" claim about `onRunCommitted` in `invalidate.ts`'s header corrected (265 files/5,107 tests green) | [link](./2026-09-11-components-dead-code-b.md) |
| 2026-09-11 | Lib DB Queries YAGNI Dead-Code Removal | Audited all 80 exports of `lib/db/queries.ts` (1,914 lines) for callers repo-wide (word-boundary greps + a dynamic-channel sweep: namespace imports, bracket access, `db.query.*`, CI — none; one `.workflows/` doc-mention false positive corrected before it drove a decision) and removed the 6 genuinely dead functions + 1 orphaned interface in 4 gated commits (`getMonthlyTotals`+`fillZeroMonths`, `getObservedMaxHr`+`getObservedMaxHrExcludingRun`, `listExtractions`, `deletePhoto`) with every companion: the `ALLOWED_UNSCOPED` guard list 4→3 and its exact-mirror test, the reviewed-only invariant-completion list 15→14→12, rollups/ownership/extractions/integration suites — 8 files, −315 lines, queries.ts → 1,777, default suite 5,107→5,093 (delta reconciled to the digit) — zero schema.ts/migration/table/column change, all six `ci:*` boundary guards green, and the kept-and-noted list (getInsight, ownership vocabulary, type-only exports) written down for the next pass | [link](./2026-09-11-lib-db-queries-yagni.md) |
| 2026-09-11 | Components Dead-Code Sweep (app/ + ui/charts/review/profile/share/trends) | First symbol-level YAGNI pass over `app/` + six component directories — earlier surveys had ruled out orphaned *files* but never asked which exports have zero callers: 9 exported symbols de-exported (Button's 5 prop/variant types, Field's 2, plus module-private `chipClasses` and `Flag`) and 6 dead barrel re-exports dropped (commit `eb93607`, 7 files, +46/−38), found by a TypeScript-compiler-API export extractor feeding a four-layer verifier (per-symbol word-boundary grep, comment/path-string classifier, multiline-aware barrel-importer scan, positive-controlled against a planted dead symbol) and confirmed by a 0-candidate post-removal re-sweep — with both loud false alarms correctly dismissed (the review/ vs ui/ `SplitsTable`+`ZoneBar` twins, relative-imported and documented as deliberate; the 42-importer barrel itself), and the Chip/Flag test pins reworked onto the public surface with coverage intact (9+8 tests, before = after; suite 265 files/5,107 green) | [link](./2026-09-11-components-dead-code-a.md) |
| 2026-09-11 | Admin PhotoReference False-Positive Fix | Small targeted fix, committed directly to `main` (no branch): closed the one follow-up the admin-explorer-upload-tests session deliberately left open — `tests/admin.photoReference.test.ts`'s 29 `readFileSync`/`toContain` source-text assertions (the day's second string-matching false positive, after `admin.mediaPane.test.ts`) moved to 3 new real DOM tests in `PhotoReferencePicker.test.tsx`, one mutation-verified, plus a stale `existsSync` phase-4/5 guard dropped and the file's now-inaccurate "no jsdom" docstring rewritten (44 → 45 tests; full suite 265 files/5,107 tests green) | [link](./2026-09-11-admin-photoreference-false-positive-fix.md) |
| 2026-09-11 | Lib Admin Action Tests | Finally closed the follow-up deferred by all 3 prior sessions: first-ever runtime execution of the admin security boundary (`requireAdmin` — redirect/notFound split, id-first order, 401/404, empty-ADMIN_EMAILS crash) and the 9 fully-untested `lib/admin` action/store files plus the 5 thrice-deferred avatar actions, ~180 tests (174 measured) in the repo's real-query-layer-over-fakeDb posture — including a real 23505 unique-violation through drizzle's error wrap via a new additive `fakeDb.enqueueError`, and honesty pins like `registerNinaAvatarsAction`'s `skipped` counting storage conflicts, not join misses (suite: 4,191 → 4,365 tests) | [link](./2026-09-11-lib-admin-action-tests.md) |
| 2026-09-11 | UI Primitives Tests | Closed the shared design-system layer's zero coverage — all 16 untested files in `components/ui` (14 primitives + the `usePanelParam`/`useSavePhoto` hooks, a gap named on prior sessions' menus and passed over every time) with 205 real DOM tests across 16 new co-located files (~2,740 lines), the rendered-scenario complement to the repo's node-env text-scan suites rather than a duplicate — including the iOS-keyboard bug's absence proven in a rendered Sheet sequence and the native dialog's no-redundant-ARIA rule (suite: 4,191 → 4,396 tests) | [link](./2026-09-11-ui-primitives-tests.md) |
| 2026-09-11 | Architecture Reference | Closed the follow-up all five prior 2026-09-11 sessions deferred (5× deferred, 0× acted on): read all 36 `docs/plans/*.md` in full (~19.2k lines), cross-checked each against the current implementation (672 commits, 28 tables, 204 lib modules, ~3.6k tests), and synthesized `docs/architecture.md` (622 lines) — the system as it actually exists, including a per-plan cross-reference (§13) marking all 36 docs SHIPPED / SHIPPED+AMENDED / SUPERSEDED with what diverged | [link](./2026-09-11-architecture-reference.md) |
| 2026-09-11 | Nina Remaining Component Tests | Closed the day's open arc in one batch session: 25 new co-located test files / 193 tests (~3,300 lines) covering every still-untested `components/nina` component — the flagged-since-session-2 search field and SessionList/SessionRow/ChatChrome/NinaAboutScreen included — with real collaborators over mocks (real `next/image`, search, crop math, `planSessionRemoval`), taking the nina directory to 31 files / 313 tests all green (full suite: 4,403) | [link](./2026-09-11-nina-remaining-component-tests.md) |
| 2026-09-11 | Admin Explorer & Upload-Hook Tests | Finished the admin component-coverage sweep in the day's first worker-mode session: first real suites for all 18 remaining untested surfaces — the two components the prior session had to stub (`PhotoMoveBar`, `FolderMenu`), the ~447-line `useFolderUpload` upload state machine (renderHook + gated promises + `@vercel/blob/client` mock, four-lane bound mutation-checked), and the admin control panels (auto-save commit matrix, escalating-poll image-gen loop, optimistic tables, `photoIcons` glyph table) — 324 tests across 18 new files (suite: 4,191 → 4,515), plus a second string-matching false positive exposed (`tests/admin.photoReference.test.ts`) | [link](./2026-09-11-admin-explorer-upload-tests.md) |
| 2026-09-11 | Admin File-Explorer Tests | Closed the admin FileExplorer subsystem's total lack of component-level coverage — flagged in two prior sessions' follow-ups as "the largest untouched real gap" — with 119 real DOM tests (RTL + happy-dom) across all 9 files (`FileExplorer.tsx` + 8 `components/admin/explorer/*.tsx`), proving a pre-existing string-matching suite was a coverage false positive (suite: 4,072 → 4,191 tests) | [link](./2026-09-11-admin-file-explorer-tests.md) |
| 2026-09-11 | Nina Message-Actions-Sheet Tests | Closed the gap named as a follow-up in all 3 prior 2026-09-11 docs and acted on by none of them: 30 sanity-checked tests for `MessageActionsSheet.tsx`'s menu/edit/retry/resend/delete paths, including the retryable-vs-non-retryable failed-message distinction and the three edit-refusal shapes (suite: 4,042 → 4,072 tests) | [link](./2026-09-11-nina-message-actions-sheet-tests.md) |
| 2026-09-11 | Admin Folder-Actions Tests | Corrected a false-positive "zero coverage" survey of `components/admin`/`lib/admin` (tests live centrally under `tests/`, not co-located), then closed a real gap: 23 sanity-checked tests for the 6 previously-untested folder-maintenance Server Actions in `ninaAlbumActions.ts` (suite: 4,019 → 4,042 tests) | [link](./2026-09-11-admin-folder-actions-tests.md) |
| 2026-09-11 | Nina Composer/Bubble/List Component Tests | Continued the day's component-testing push onto the 3 files the first session flagged as untested — `Composer.tsx`'s upload/dedupe pipeline, `MessageBubble.tsx`'s gesture wiring, and `MessageList.tsx`'s grouping/composition logic — adding 48 sanity-checked tests across 3 new files (suite: 3,971 → 4,019 tests) | [link](./2026-09-11-nina-composer-bubble-list-tests.md) |
| 2026-09-11 | Nina Chat Component Tests | Stood up React component testing from zero (RTL + happy-dom installed and wired into Vitest) and wrote 23 sanity-checked tests covering `ChatScreen.tsx` and `NinaSidebar.tsx`, the app's two largest/highest-churn UI surfaces, previously covered only by manual testing | [link](./2026-09-11-nina-chat-component-tests.md) |
